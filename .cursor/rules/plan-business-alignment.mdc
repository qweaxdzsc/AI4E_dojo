---
description: 写或修改实施计划时加载：先按做什么/怎么做写总结，需要时加分点讨论，再在实施评审里列功能点并对主功能写 BDD。架构设计、选型对比、范围澄清、纯讨论不要加载。
alwaysApply: false
---

# 实施计划装填骨架

只用于已决定落地的实施计划。架构设计、选型、范围澄清、纯讨论不套本骨架；不是实施计划则写明「不套本骨架」。按节标题装填当次内容，不在 rule 里写业务事项。

顺序：业务描述总结 → 讨论（按需）→ 实施评审。不得混排。上半用产品语言，禁止裸技术标识；绕不开时 `` `标识`（中文含义） ``。不是实施重点的内容不要写进计划。下半标识必须带中文；除接口、讨论外，某节无内容写「本次无此项」及原因。

---

## 上半：总结 + 讨论（按需）

### 业务描述总结

分点写，不要一段散文化。思想和结论在这里定死，后面不得另起说法。

- **做什么**：给谁、解决什么、做成后可见什么。
- **怎么做**：关键对象怎么分工、怎么交接。

### 讨论（按需）

实施里需要把未定或取舍说清楚时才写。无讨论则整节不写。

内容不限，但必须分点、有小标题或编号，一段一事。不要写成一团散文。这里只讨论，不定文件、不写步骤。

---

## 下半：实施评审

### 功能点

用层级树，三级各干各的，不要混在同一列：

1. **顶层同级**：主功能 A、主功能 B、辅助 C……彼此并列。辅助不挂在某个主功能下面。
2. **主功能块内先 BDD、后子功能**：BDD 单独一块；子功能另起「子功能」列表。Given / When / Then 不得与 A1、A2 写在同一组子弹里。
3. **子功能**：只挂在所属主功能下，一句极短，不写 BDD。

只给主功能或特别重要的点写 BDD。一个 When 一件事。功能名用人话。错：「校验后得到可继续的数据上下文」。对：「原始数据的数据校验」。

```text
主功能 A：[稍长的业务句子]
  BDD
    Given ...
    When ...
    Then ...
  子功能
    A1：[做什么]
    A2：[做什么]
辅助 B：[做什么]
```

一叶至少一条测试。

### 接口（按需）

不是实施重点则整节不写。不要为列而列。

若要写：只写模块之间或主业务对象之间、且这次真正要落地的约定。每个接口一小段，段内用极短句子带过**位置**、**作用**、**输入**、**输出**。不要把这四项平铺成清单。段与段空一行。

### 架构改动

分点写「改了什么 / 为什么」。没改的不写，不用长段复述原架构。

### 文件改动

用目录树写新增 / 修改 / 删除 / 移动。每个叶子带中文职责，并对上某条架构改动。

### 实施步骤

一步一行：做什么 + 完成标准。不展开接口、不复述功能点。须覆盖功能点各叶。

### 测试用例

与叶子一一对应。前提、动作、期望，以及测试位置（带中文说明）。

---

## 注意事项

- 用角色、动作、对象、结果说话。
- 例子只示范结构；填写时换成当次任务。

---

## 填空骨架（虚构例子）

### 业务描述总结

- **做什么**
  - 提交人一点「生成本周周报」，得到一页可读完整稿。
  - 材料不齐时拒绝出半成品，并列出还缺什么。
- **怎么做**
  - 清单对象先判断齐不齐；不齐则停。
  - 齐了由汇稿对象收材料，成稿对象出一页。

### 讨论

- **半成品要不要先出一版**
  - 不先出。缺材料时只告诉缺什么，避免发出去的稿和目录对不上。
- **说明放哪**
  - 跟人看的应交清单单独放，不塞进生成入口。

### 实施评审

#### 功能点

**主功能 A：按周生成完整周报**（一点即可得到一页可读稿）

BDD：

- Given 提交人已交齐本周材料，且本周还没有周报
- When 提交人选择「生成本周周报」
- Then 得到一页完整周报，材料与目录能对上

子功能：

- A1：材料不齐时拒绝

**辅助 B：查阅应交说明**

#### 接口

汇稿给成稿的入口，在 `report/assemble.py`（汇稿）：周次和提交人进去，出来一页周报；材料不齐则带回缺什么。

#### 架构改动

- 拼稿从人手工改为汇稿 + 成稿：避免漏页、对不齐。
- 增加清单对象拦在成稿前：避免出半成品。

#### 文件改动

```text
report/
  assemble.py              新增（汇齐本周材料）
  render.py                修改（出一页稿）
docs/
  weekly-checklist.md      新增（应交材料说明）
tests/
  test_weekly_report.py    新增（覆盖 A、A1、B）
```

#### 实施步骤

1. 齐不齐可判断 → 不齐能列出缺失
2. 齐全时可出一页稿
3. 补说明，并跑通 A、A1、B

#### 测试用例

- A：材料齐全时生成 → 得到完整周报；`tests/test_weekly_report.py`（周报生成）
- A1：缺一份材料时生成 → 无周报且有缺失清单；同一文件
- B：打开说明 → 能看到应交项；核 `docs/weekly-checklist.md`（应交说明）

---

## 反例

- **不是人话：** 满篇路径和标识；功能点像从代码翻译出来（「校验后得到可继续的数据上下文」）。
- **套错场合：** 整份计划写成架构讨论；或讨论区没有小标题、写成一团。
- **结构不合格：** 总结一段铺开；功能点平铺无主次；BDD 与子功能写在同一组子弹里；辅助挂在某个主功能下；子功能也写满 BDD；把不是重点的内部函数当接口四行平铺；架构复述未改部分；文件扁平罗列；步骤写成小段。

---

## 修改与验收（计划里必须写明）

修改用户可编写的脚本、配置或扩展入口时，计划必须直接展示修改后的典型文件写法，明确步骤、参数来源、输入输出和执行时机。示例须区分已有接口和拟新增接口。

计划必须说明用户如何修改参数、插入步骤、替换能力和消费新增输出。验收包含用户在复制目录中的真实扩展，不能只检查注释、代码结构或 import。

涉及职责变化时，同步相关开发规则和 AGENTS.md，清理冲突的现行表述。

与根目录 `AGENTS.md` 的修改与验收纪律一致。实施步骤和文件改动必须覆盖这三条，缺一条则计划不合格：

1. **上下游整条链一起看。** 写清谁提供输入、谁消费输出、中间交接什么、失败时谁感知。不得只计划局部改动。
2. **同一改动必须带齐。** `AGENTS.md`、`.context`、相关 PRD（`docs/PRD/`；没有则补）、测试用例。目录或职责变了还要改模块索引。
3. **用相关测试验收。** 先圈定本次影响到的用例并跑通，才算验收。不跑全仓冒充验收；尚无对应测试则先补再跑。
