---
description: 撰写或修改 docs/PRD 时加载：按 package 再按包内模块分文件，每章节必须有 1.1–1.6，清单与详解共用独立数字编号。
globs: docs/PRD/**/*.md
alwaysApply: false
---

# PRD 书写硬约束

完整规范见 [`docs/PRD/README.md`](../../docs/PRD/README.md)。本 rule 只列不得破的约束。

- 目录为 `docs/PRD/{包}/{模块}/PRD.md`。包名对齐 `packages/`，模块名对齐该包一级目录（core 为 `base` / `abilities` / `applications` / `run` / `tools`）。
- 禁止扁平切片，禁止按能力层编号建顶层目录。读取、下载是 `abilities`「数据」章的功能点，不得升成独立 PRD 或独立「一、二」功能域。
- 未交付的包或模块不建空目录；只在 README 对照表标「待编写」。`abilities` 按 data / transform 等分章，未交付分组不写空章。
- 每个章节必须有且仅有这六节：背景与定位、核心业务操作流程图、业务状态机、状态值说明、功能点清单、功能点详解。无状态也保留 1.3 / 1.4，写明「本章无业务状态」。
- 不要写数据模型、接口清单、验收标准；代码路径写 `.context`，验收由 `AGENTS.md` 圈测试。
- 1.1 写清边界（不做什么）。禁止旧顶层：用户结果、做什么、不做什么、功能点、上下游、验收。
- 1.5 与 1.6 共用独立功能点编号（`1.` / `1.1` / `2.`），只在清单和详解里另起，不延续章节标题 1.1–1.6。清单不用无序号的点；详解每个功能点、子功能标题前必须带同一编号。
- 1.6 按该编号切开，条目说明作用、设计原因、必要使用约定、失败边界及迁移影响；配置示例和结果含义归入对应功能点。不写内部算法步骤或 UI 实现。
- 改功能时同步受影响的模块 PRD；新增、删除或移动模块目录时同步 README 对照表与 `.context`。

- PRD 是模块功能的长期唯一正文；其他接入说明只做导航。每次改动更新现行正文，不在文末无归属地追加摘要。跨模块分层仍由架构文档维护。

- 普通 `recipes/` 模板集合不再安装；其 PRD 使用 `docs/PRD/recipes/{案例}/PRD.md`，作为 package 目录规则的明确例外。
