---
description: Recipe 显式流程、配置可读性与用户能力扩展规范
globs: recipes/**/*,examples/aero_cfd/**/*,examples/gencp/**/*,examples/safediffcon/**/*,examples/recipe_extensions/**/*
alwaysApply: false
---

# Recipe 编写规范

- 每次改动评估 Agent 帮助影响，遵循 `AGENTS.md` 的帮助同步检查：公开能力/签名/输入输出/状态/组合及 recipe 用法变化，同步 Docstring、`docs/agent-help/` API 与教程，按需更新 `DOJO_AGENT_GUIDE.md`；案例 README 与清单描述须一致。重新生成帮助并检查漂移，验证相关例子、检索及受影响的 wheel/离线导出；使用约定不变可说明不适用，不机械改入口文案。

- Recipe 是研究者可以编辑的流程正文，必须表达实际步骤、顺序、参数来源和结果交接。
- Python 决定顺序，YAML 提供参数与能力选择，不添加另一份 YAML 步骤列表。
- 不以“极薄”“只有几行”为目标，不把新模板写成整段 workflow 转发。
- application 提供业务步骤与领域绑定，ability 实现计算，run 提供通用执行和运行记录。
- Recipe 不实现数值算法、样本循环、训练循环，不修改私有上下文字典绕过公开接口。
- 明确区分登记与执行，保留采样等操作的真实执行时机。
- 自定义能力使用普通函数或对象，支持直接传入和全限定路径；持久化仅保存可重建声明与来源，不保存闭包。
- 参数严格校验，扩展参数允许在复制案例中声明，不要求修改框架注册表。
- 新字段和变换必须交接实体身份、单位、状态，并验证保存、读回及下游消费；筛选同步更新字段与实体身份。
- 允许案例存在明确的局部流程差异，不为脚本字节一致而隐藏流程。
- 官方模板不增加空组件占位目录，真实扩展示例可包含实际能力文件。
- 可读性验收包含仓库外复制、真实扩展、错误定位和结果核验。
- 示例：提取后通过 `map_fields(..., inputs=..., outputs=..., operation=...)` 登记新字段；随后在选择、统计、归一化和模型绑定中使用该字段。登记本身不执行样本计算。

- 原始处理默认值和能力描述来自数据组件声明；页面、脚本和检查共用配置解释，显式空选择不回填。新入口按样本名单解析依赖，逐场输出与历史容器分开，清单必须反映实际交付。

- 外流新增独立 `infer.py`：在 Python 正文登记恢复、预测、物理输出、评价、保存与网格交付；新 `post.py` 消费固定结果。`pipeline.stages` 只选择执行范围，不能成为另一份步骤顺序或算法实现。
- 推理参数属于 infer，不能通过修改 train.device 或原始数据选择来指定本次推理。配置加载、覆盖、路径、模型查询参数、捕获快照和复制案例必须一起适配。
- 原 post 调用入口保留历史执行语义；新流水线缺推理结果必须报错，不回退成模型预测。平台按任务公开操作声明调用；来源摘要仅作记录，不以模板全文差异拒绝可复制研究脚本。
- 自定义预测与派生字段通过公开步骤注入；字段须保存并读回实体、单位和有效性，交付到后处理。新代码不能只把可编辑流程包装成一个全量调用。

- 新模板明确登记物理量/分量选择，YAML参数必须实际影响评价和保存；不重写历史模板指纹。

- 物理场输出须在脚本正文表达评价、分析、绘图和保存顺序。训练观察只负责选择时机并显式调用 infer 快照与 post 步骤；不在原子能力中调度轮次。自定义绘图可注入普通函数，新增字段必须保存读回。

- 共享输出与阶段输入只约束 Task 托管入口；脚本正文仍表达步骤与算法连接。连续执行必须分开物理共享目录、归一化副本与预测目录；公共输入采用 inputs.<stage>.<name>，输出采用 run_root/data_root，不把流程搬进 Task。

- GenCP 模板显示逐场训练、单场验证与多场条件/积分绑定；后处理消费固定分场结果。场顺序在 Python，条件与构造器可用普通函数替换。缩小对照与完整论文复现分开报告，累计预算包含同组失败重试。

- 控制recipe显式交接预训练、校准/后训练、适配和真实响应；源码目标与数据targets分开，post不重跑求解器。缩小预算与论文精度分别报告，失败与重试计入同一案例累计预算。

时空预测recipe显式交接物理轨迹、模型准备、训练、固定权重预测和独立评价；派生数组须保留样本身份并说明单位与轴。原权重导入与完整状态续训分别声明，缩小对照不等同论文复现。

采用公共研究配置的模板由直接脚本与Task运行同一pipeline.py；inputs只声明阶段外部路径，data_root是运行数据根。使用TrainingRun公开目录/资产/指标入口，不复制任务流程或新增逐案例task-entry。旧配置转换和历史产物兼容分别说明；正常开训和恢复的跨进程对照不能用导入检查代替。

- 公共约定统一路径和交接，不要求不同领域脚本正文相同。显式外部输入与上游返回值冲突时报错；缺管理索引不阻止自由脚本执行，但不冒充已完成研究或可比较结果。

- 研究任务先按 `.context/tasks/research.md` 定位最小阅读与修改集合。共享训练内部调整不要求批量改写原模板；新增构造/更新选择只在实际消费者贯通。扩展覆盖集先覆盖完整模板，验证实际数值变化、恢复与固定结果读回，不能仅检查导入。

- 准备、训练、推理在 recipe 中明确连接同一科学链；trainprep.topology 只控制图缓存派生，不决定整条科学流程。公共模板与 AB-UPT 使用锚点链，Transolver 与 MeshGraphNet 使用物理链；MeshGraphNet 缺必需拓扑时明确拒绝。
