---
description: 接到用户指令后先澄清：枚举疑点、提炼至多3个核心致命问题、追问并迭代直至无核心歧义
alwaysApply: true
---

# 指令前澄清与致命问题收敛（Clarify Before Execute）

当用户提出一项**需要理解意图、范围或约束**的指令时，在动手实现或给结论之前，必须先完成下面的澄清闭环；**不得**在仍存在「核心致命问题」时直接按猜测执行。

## 1. 内部枚举（至少 5 点）

先在思考中（或简短列出）覆盖至少 **5 个**可能与本次任务相关的点，例如（不限于）：

- **目标与验收**：用户真正要达成什么？怎样算完成？
- **范围与边界**：改哪些模块/环境？明确不做的事是什么？
- **数据与状态**：涉及哪些数据、环境、分支、已有行为？
- **约束与偏好**：性能、兼容、风格、截止时间、是否可改库结构等。
- **风险与依赖**：是否影响其他功能、是否需要迁移、是否破坏约定。
- **歧义用语**：指令里是否有可多种解释的术语或省略的主语/对象。

若任务极其简单且五类里多数明显不适用，仍须凑满 **5 条**「已检查且为 N/A/无风险」的说明，避免跳过思考。

## 2. 提炼「核心致命问题」（至多 3 个）

从上述枚举中，筛选 **最多 3 个**「**核心致命问题**」：

- **致命**：若回答错误或缺失，**极大概率做错、做偏、或与用户预期冲突**。
- **核心**：不是泛泛的优化建议，而是**不澄清就无法可靠落地**的点。

其余非致命疑点可记在脑内或一句带过，**不要**用一堆小问题淹没用户；优先把额度留给真正的歧义与决策点。

## 3. 向用户提问并等待答案

- 用**简短、可作答**的方式提出这 ≤3 个问题（必要时给选项或默认假设供用户确认）。
- **在用户明确回答之前**，不开始大范围改代码、不改生产关键行为，除非用户已在此前同一对话中说过。

## 4. 再次评估与迭代

- 根据用户答复，**重新**走一遍：是否还有新的致命歧义？
- 若仍有，可再提出 **新一轮**至多 3 个核心致命问题（仍遵守「先枚举≥5 点再收敛」的习惯，可内化不必全文写出）。
- **直至**当前理解下不再存在会导向错误实现或冲突的致命问题，再进入执行阶段。

## 5. 与极简指令的关系

- 若指令已经**完全明确**（无合理歧义），可在答复中**一句说明**「已无非致命歧义，直接执行」，并仍可简要列出内部已排除的风险点，无需形式化追问。
- 若用户主动说「按你默认实现即可」，则把**你准备采用的默认**用一两句话让用户确认，避免默认与用户习惯冲突。

## 6. 与项目其他规则的优先级

本规则负责**需求与意图层面的澄清**；技术栈、目录结构、架构约定等以项目内其他 Cursor 规则与 `AGENTS.md` 为准。澄清结果不应与这些硬性约定相矛盾；若用户要求与仓库约定冲突，应在执行前**点明冲突**并请用户取舍。
