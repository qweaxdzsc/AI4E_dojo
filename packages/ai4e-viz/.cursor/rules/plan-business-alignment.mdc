---
description: AI4E_Vis 方案规划必须对齐业务模块、持久化、迁移与逐模块测试
alwaysApply: false
---

# AI4E_Vis 计划规范

计划先说明用户可见业务结果，再列技术实施。功能范围以用户当前指令和 `docs/PRD/` 为准，代码现状从 `.context/` 导航验证，不把目录占位当作功能实现。

每份实施计划必须说明：

- 涉及哪个一级业务模块，是否触及其公开前后端门面。
- Router/Application/Domain/Repository 或 Page/Hook/Component 的职责变化。
- SQLite 表所有权、storage key、文件落盘、迁移、备份和回滚影响。
- 跨模块依赖方向及为何不能放入 Infrastructure 或 `visEngine`。
- 现有 API、HashRouter、Trame、O3DV、报告导出与数据兼容边界。
- 每个模块迁移后的独立测试，以及全部迁移后的契约、架构、构建和 E2E 回归。
- 删除旧文件前的引用归零证据和 Git 恢复点；运行数据库与上传资产不得当缓存删除。
- 公开代码的中文 Docstring/JSDoc 与复杂流程注释验收。

每份实施计划都把 `.context`、相关 PRD/变更记录、测试用例和错误记录联动列为正式 Todo；实现前必须沿需求的完整前后端与持久化链路核对影响面。


## 正式发布与宿主 Web 冒烟（硬规则）

- 每次实施计划必须包含「发布到实际使用入口」与「Agent 自己做正式 Web 冒烟」，遵守根 `AGENTS.md` 同名硬规则；只改文档时明确记录不适用及原因。有宿主消费链的 Vis 改动不能免。
- Vis 使用 force-include 安装副本，改 `packages/` 不等于正式生效。未对 `ai4e-viz` 执行规定重装并回收/重启旧 Vis 子进程时，正式 Web **验收不了**。核验后端及 frontend/dist 摘要、正式服务实际安装路径与启动身份；授权后按根依赖组纪律更新必要包，回收旧 Vis/Trame，核对浏览器实际返回新版资源。不得顺带重装无关包或删除数据库、上传资产。
- Agent 必须从用户实际 Dojo 5173→8000 进入 Vis → Trame 双层嵌入，逐项操作本轮受影响功能并核对最终画面；本地/远程渲染均受影响时分别验证。独立 Vis 页面、隔离 7999/5172、源码测试或标「待发布」不能代替正式宿主验收。
- 对应改动必须覆盖其实际行为：着色看模型与色标；子对象应用看父子隔离；平面拖动看命中、手柄与相机稳定；删除看残影、隐藏对象与背景；设置看保存重开；透明导出看下载文件 alpha。记录实际截图、版本及逐项结果，不以状态字段正确替代画面正确。
- 未获当次同意仍不得擅自重装/重启正式 8000/5173，必须当场申请。在重装+Vis 会话更新+Agent 正式冒烟完成前只能是未验收。「待发布」不得当作交付结束。已获明确授权则继续完成发布及验证。
