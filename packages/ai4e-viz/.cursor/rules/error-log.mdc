---
description: 日常编码中如何把被纠正/返工的原始错误记录到根目录 error.log。只追加、不提炼。
alwaysApply: true
---

# 错误日志记录规则

把"被纠正 / 返工"的原始事实，简短追加到根目录 `error.log`。
这是原始矿石，不是方针——这里**只记录，不总结，不提炼**（提炼是 `@distill` skill 的事）。

## 何时记（满足任一，且确实是被核实过的错误）

- 我明确纠正了你的做法
- 出现返工 / 代码被推翻重写
- 测试、校验或 review 抓到了问题
- 反复在同一类地方卡住

**不要记**：正常的迭代探索、纯风格偏好、还没被验证为"错"的东西。
门槛是——"人核实过的真实错误"。宁可漏记，也不要把日志灌成流水账。

## 怎么记（追加到文件末尾，**绝不改写或删除已有条目**）

```
## YYYY-MM-DD
- 类别: <短标签，尽量复用已有标签，便于聚类>
- 场景: <一句话上下文>
- 错误: <做错了什么>
- 正确: <正确做法是什么>
- 状态: 未提炼
```

类别标签示例：`跨模块契约` / `三层边界` / `JSON契约` / `流程设计器` / `状态管理` / `脚本幂等` / `CAE调用` …

## 约束

- 简短、事实性，一条就几行。
- "类别"标签尽量和已有的对齐——复发判断和聚类全靠它。
- **不要**在这里写"以后要注意…"式的总结或规则。
- **不要**改 `.cursor/rules/lessons.mdc`。归纳和上升的活儿，留给 `@distill` 提炼流程。
