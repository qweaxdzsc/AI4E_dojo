---
description: ZK_Agent 作战方针集（从反复出现的错误实践中提炼的根上编码方针，每轮加载用于指导决策）
alwaysApply: true
---

# 作战方针集

这里是从**反复出现的错误实践**中提炼、并升到最高层的**根上编码方针**——一份薄册子，几条根上的话，
每条一两句、适用条件焊进措辞，读完就知道下次该怎么做。

这不是日志，也不是流程说明。**如何记录错误、如何提炼晋升、维护纪律与条目格式，全在 `@distill` skill 里**，
不要写进这里。本文件只回答一件事：**编码决策时，根上的方针是什么。**

条目须经 `error_distill.log` 筛选池复盘验证；升到第三层且经你确认后，才毕业写入本文件。
「跨模块/跨类」等是 L3 晋升的耐用度判定条件，不是本文件的设计目的。

本文件只由 `@distill` 流程在你确认后修改。

---

（暂无正式作战方针。由 `@distill` 从 `error.log` 晋升后写入。）
