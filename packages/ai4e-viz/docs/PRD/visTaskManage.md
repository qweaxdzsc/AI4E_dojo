# 可视化配置与修订

## 目录

- [一、可视化配置与修订](#一可视化配置与修订)
  - [1.1 背景与定位](#11-背景与定位)
  - [1.2 核心业务操作流程图](#12-核心业务操作流程图)
  - [1.3 业务状态机](#13-业务状态机)
  - [1.4 状态值说明](#14-状态值说明)
  - [1.5 功能点清单](#15-功能点清单)
  - [1.6 功能点详解](#16-功能点详解)

## 一、可视化配置与修订

### 1.1 背景与定位

为物理场与原有图表方法提供配置管理、推荐、参数校验和版本兼容。配置声明描述如何重建结果，工作会话和原数据由对应模块管理。

### 1.2 核心业务操作流程图

```mermaid
flowchart LR
 A[选择方法或读取配置] --> B[校验来源和参数] --> C[提交新配置修订]
```

完整流程：选择方法或读取配置 → 校验来源和参数 → 应用工作区 → 提交新配置修订。

### 1.3 业务状态机

未保存 → 已保存 → 新修订。冲突时保留旧修订，要求刷新后重试；失败不覆盖历史配置。

### 1.4 状态值说明

成功表示本次操作完整交付；失败保持原可用结果。未完成或被取消不得标记为已完成。本章不额外建立全局状态或数据库。

### 1.5 功能点清单

物理场版本 2 分离 sources、pipeline、layers、probes、views、layout、link_groups/time；稳定身份独立于数组位置。计算字段与着色字段分开；视图具有各自显示实例。版本 1 在内存转换，补基础显示与等价布局，历史文件与摘要保持不变。图表仍使用原版本及渲染实现。允许移除全部结果后的空物理工作区；不允许悬空处理依赖。流线缺起点类型按线段读取；命名面对象引用必须已在管线中且无环。切面不新增配置字段。

### 1.6 功能点详解

修订只保存来源引用与参数。草稿、选择、端口、可视平面和原始数组不进入配置。预期修订防止并发覆盖；只写目标任务位置，不创建研究版本。旧流线只有起终点时仍可打开并按线段计算；配置版本仍是 2，不改历史修订。
