import {test,expect} from '@playwright/test';
import {inferenceFixture} from './inference-fixture';
test('表格图表与导出固定同一结果，切换不再次预测',async({page})=>{const state=await inferenceFixture(page);await page.goto('/projects/p/tasks/t/infer');await expect(page.locator('[data-region="table"] tbody tr')).toHaveCount(5);for(const name of ['散点图','柱状图','折线图'])await page.getByRole('button',{name,exact:true}).click();await page.locator('[data-region="chart"]').getByRole('button',{name:'配置'}).click();await page.getByRole('button',{name:'应用',exact:true}).click();const download=page.waitForEvent('download');await page.getByRole('button',{name:'导出 CSV',exact:true}).click();await download;expect(state.submitted).toHaveLength(0);expect(state.exports[0].selection).toEqual({field_id:'surface:pressure:scalar',metric:'relative_l2',split:'validation'});});
