# 平台一致性修正验收（2026-09-14）

本记录只覆盖本次 Web/Server/Task 修正，唯一布局参考为 `docs/prototypes/dojo-web-integrated.html`。仓库启动时已有算法、recipe 与其他专项改动，本切片保留它们；未修改网络、训练循环或案例配置树。

## 实现与验证

- A1/A2：任务表与工作台共用正式运行摘要；浏览不标记前序完成，检查关联配置/输入修订，未知历史证据不补造成功。后端一致性与阶段浏览器用例通过。
- B1/B2：检查、试跑、执行和结构生成先保存；冲突保留草稿、不提交；稳定请求身份支持响应丢失重试。保存和提交不是跨请求原子事务。绑定取消不写入，确认绑定使用保存后修订。
- B3：PT/Zarr互斥、VTKHDF独立附加。两数据集×两格式共四组真实处理、独立数值预览与现有准备读取通过；不是四模型训练验收。
- B4/B5：明确恢复配置绑定，包括受控外部来源；失效路径返回位置与原因。候选不自动选最新，也不预先声称兼容；实际检查仍消费原算法门面。
- C1–C3：准备输入、准备结果和后处理结果按固定清单/运行浏览；文件树展开、匹配选择、刷新保留有效选择；Zarr独立资产预览与ZIP下载保留元数据。旧预览和新资产共用内容修订。
- D1/D2：1440/1920首页、任务列表和工作台关键位置按1 CSS像素门槛通过；模型与训练控件的高度、间距和列宽通过。整页记录覆盖rawprep/model/train/post共8个实际页面状态。
- D3：真实epoch坐标、不规则间隔、单点与缺失值不补零；日志搜索、已有级别、滚动、显示清空与完整下载通过。图形边界用明确组件夹具验证，真实运行日志由页面执行覆盖。
- D4：后处理配置折叠、固定运行文件、三维页三栏及场景入口已接。整页几何使用真实未执行任务空态；真实VTK文件的转换、着色、过滤、多窗口、联动、保存与资源释放另由真实查看器用例通过。尚未逐像素验收“已预测网格+展开配置+多个窗口”的全部组合。
- E1–E3：受影响后端接口/资产/配置/执行/文档/架构回归、前端构建、微领域边界与生成类型通过；本记录不使用旧测试数作为当前证据。

## 当前证据

- `backend-final.xml`：主Agent统一运行16个相关测试文件，85 passed，0 skipped。
- `real-handoff.json`：新增真实PT/Zarr四组交接，4 passed，0 skipped；逐组列出外部实验目录、运行身份、预览形状和准备读回字段。原始证据与产物保存在指定dojo_train目录。
- `browser-final.json`：20 passed，0 skipped，0 flaky。包含真实首页点击/创建绑定、两类数据页面执行与预览、状态/错误夹具和原型对照。
- `viz-final.json`：1 passed，真实ShapeNet表面文件经服务/viz/浏览器，非示意图片。
- `browser-artifacts/`、`viz-artifacts/`：本轮自动浏览器产物。
- `whole-page-measurements.json` 与 `*-actual.png` / `*-prototype.png`：双视口整页尺寸与图片，具体文件见本目录。
- 前端 `npm run --prefix packages/ai4e-web build`、`check:architecture` 通过；圈定Python Ruff通过。构建仍提示现有主包大于500KB，不影响本轮功能验收。

## 已核实故障与修复

1. 真实输入旧临时路径已经不存在：显式改验收环境为持久数据根，未造替代数据。
2. 仓库显式案例改版后，平台只复制example YAML仍用通用recipe脚本：NASA运行报`open_dataset`缺失。现在通过task公开模板门面复制对应example原脚本；新建四案例脚本摘要测试与真实NASA运行通过。已有任务不自动换脚本，不改写历史快照；曾以错误脚本创建的NASA任务应重新从已登记案例创建。
3. 旧预览按单文件计算修订，Zarr目录失败：收拢为公共目录内容摘要，分页/缓存失效/符号链接拒绝与真实Zarr预览通过。
4. 任务页签高度偏差2.796875px：修正字体行高，原1px断言未改并通过。

## 明确边界

- 1440模型面板真实参数数量影响内容高度，记录为业务差异；外壳、820px滚动窗口和控件尺寸仍按原阈值测量。
- 未把组件夹具算作真实计算，没有重跑无关生产训练；没有新增算法、升阻力指标或完整ParaView功能。
- 未覆盖全部错误态的逐像素截图、所有历史准备记录格式与所有预测网格状态。缺字段/修订/兼容证据保持不可确认。
- 报告、批量、队列和并行仍未开放。没有人工手点验收。
