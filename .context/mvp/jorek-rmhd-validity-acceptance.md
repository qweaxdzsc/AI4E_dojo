# JOREK RMHD 双组实验验收

2026-09-21。状态：工具已实现并进入真实正式运行；五轮与隐藏评价尚未完成，不代表正式实验已交付。

比较根：`/Users/zonghui/work/project_simulation/dojo_train/jorek-rmhd-comparison/comparison-53276f13-59a1-439d-9414-e5da03d21731/`。

## 已验证

- 原始100轨迹按PCG64(42)整轨迹70/15/15切分；来源SHA256核对。主控预实验仅用train/validation，完整500epoch、2500更新。
- MPS训练含定期验证546.7487秒（9.11分钟），训练计算及加载489.1048秒，准备4.0413秒。
- 六场等权验证相对L2 0.5123239281，保持最后帧0.5765333739；rho分别0.2867698081/0.3863035152，T分别0.3123440733/0.3368914392。科学门槛全部通过。
- 常驻完整40帧进程内P95 6.7764ms；外层沙箱、父进程可信计时含通信拷贝复放P95 8.6566ms，精度与原预实验差小于1e-6。恶意读取私有哨兵和连接本机8000均被拒绝。
- 两个实际组根跨组、父目录、链接、子进程、公共网络与MPS预检通过。网络只经主控公网代理，系统GPU驱动只读，无整个Homebrew或宿主用户目录读权限。
- CLI验收会话 `01a0c291-e599-7331-b98b-cd61579441f8` 只执行pwd和算术，不是第三个实验组。同ID续接、真实终端工具和供应商逐请求usage均成功。初次code-mode helper读取不足已改为精确文件放行，续接无该错误。
- 50项圈定测试通过（RMHD新测试19项加Neumann回归31项）；后续变更需复跑受影响集。

## 正式组

- 白板组：`jorek-rmhd-plain/experiment-476bdae5-ae57-488a-96fe-0ee1b9abe4c6/`。
- Dojo组：`jorek-rmhd-dojo/experiment-92b0e57f-5e77-4726-9efc-6b393e8e4f6a/`。
- 会话绑定各自组级根。初始材料为相同原始分片、网络与权重，独立安装包副本；没有前处理代码、张量、统计或缓存。环境由会话自行创建。
- 双方round00和round01已冻结；白板round02已冻结，Dojo round02执行中，正式会话ID及所有尝试见比较根`sessions/`与`evidence/sessions/`；主控不向其提供预实验结果。

## 尚需完成

双方独立round00、五轮冻结、验证选最终、独立隐藏评价、完整成本审计与曲线报告。公开数据可能被再次找到，须审查下载和源码来源；TLS目标记录不能证明绝对无泄漏。JOREK同问题数值求解器未测速，不报告CAE加速比。正式8000/5173和主环境没有重装或重启。

协议更正记录：初始交付说明漏写主指标完整公式，已在白板固定round00期间、Dojo正式启动之前、任何优化和隐藏评价之前，向双方SUBMISSION追加同一六场等权FP64相对L2及非法输出规则。前后摘要保存在比较根evidence/protocol-clarification-01.json；baseline训练设置未变。

Dojo交付匹配补核：564个wheel内Python文件与组内源码逐字节一致，wheel内GUIDE也与交付入口一致，mismatches为空。正式会话：白板01a0c296-4725-7712-ae15-6a9e4785b9fc，Dojo01a0c2a5-3794-7980-abe1-45109190d223。

主控续接权限修正：旧实现读取组内protocol授权，实际初始摘要未变、已生成策略未越界。Dojo round01自然结束并冻结后，在下一调用前暂停主控、保留两个session ID，改为主控配置授权及组内协议初始摘要核验后恢复；没有中断训练。证据为authority-before-update-audit.json和controller-authority-update.json。

计时范围：最初两组round00及Dojo round01的主控验证缺少独立execution区间，仅可从主控文件时刻估计并单列。后续已增加单调时钟起止记录，窗口起点不会覆盖计时变量。训练进程内未单独记录的验证仍须在最终审计标记范围，不将其作为纯训练时间。

对称说明补充02：已向两组SUBMISSION加入相同中性文字，说明自行完成可复用已有库和工具，不要求重新实现；发生于白板baseline冻结后、Dojo round00期间、任何优化之前。完整前后摘要与时刻见protocol-clarification-02.json。

真实参数量核验：无真值隔离worker遍历已加载预测闭包中的nn.Module，去重共享参数，并单列无法观察的自定义对象。白板round02在主控验证中加载三个网络，共4,442,398参数；通信握手及预测接口保持不变，完整预测通过50ms门槛。复合候选的JSON检查点与实际多个PT权重文件体积分别报告。
