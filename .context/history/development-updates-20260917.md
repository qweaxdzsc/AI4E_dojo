# 开发进度历史（截至 2026-09-17）

本页保存从根开发入口移出的原文，按日期和范围阅读；当前边界以根 AGENTS 和模块源码为准。

推理页目录预加载与过期批次误报（2026-09-17）：进页先填第一份兼容检查点的样本/物理量/指标，再后台核对其余；一致则继续共用，失败或不一致则提示并回退先选检查点，成功加载清掉过期红条。Checkpoint/物理量/指标提供全选。缺准备或已清理批次返回明确中文原因，不再说成「数据根未配置」。圈定 `test_web_inference.py`、`e2e/inference-selection.spec.ts`。5173 读源码即可看到前端；正式 8000 须重装 `ai4e-task`/`ai4e-server` 并重启后，缺文件接口才换成明确文案。未授权不得擅自重装。

后处理结果文件来源（2026-09-17）：结果文件树同时列出推理固定结果与训练/独立推理写在任务 `data_dir` 的预测、网格、导出；检查点、日志、原始处理和数据准备副本不进入此树。评价指标仍只认已提交清单，训练未写清单则只出现在结果文件。空态说明需先推理或在训练设置打开写出。圈定 `test_task_post_results.py`、`test_web_post_results.py`、`e2e/post-files.spec.ts`。正式 8000 须重装 `ai4e-task`（及若已装旧副本的 `ai4e-server`）并重启后，5173 才能看到新来源；未授权不得擅自重装。

训练结束写出预测/网格（2026-09-17）：训练设置增加写出预测、写出网格、写出分片，默认关闭；只开网格拒绝。成功结束后复用独立推理保存正文写入本次 `data_dir/infer/`，不创建推理批次、不解开 `evaluation_enabled`。圈定 `test_train_export.py`、`test_task_post_results.py`、`e2e/stage-consistency.spec.ts`。正式 8000 须重装 `ai4e-core`/`ai4e-task`/`ai4e-server` 并重启后，写出与列举才生效。未授权不得擅自重装。

训练运行实时曲线与评估（2026-09-17）：每个轮次结束覆盖写入训练报告，运行页即可画已完成轮次的 Loss，并分列在线分项与测试评估；不从日志拼数。官方案例默认仍关闭测试评估，训练设置写明关闭时仍有曲线和在线分项。圈定 `test_train_loop.py`、`test_web_platform_operations.py` 指标读取与 `e2e/execution-monitor.spec.ts`。2026-09-17 19:35 用户授权后执行 `uv sync --group dev --group visualization --reinstall-package ai4e-core`，只重启正式 8000（pid 46815，未动 5173 pid 23629）。同日 19:38 在 `http://127.0.0.1:5173` → `8000` 首页任务开训 run `e066c799ce4048368c3d443392eead17`：第一轮结束时运行仍为 running，`training.json` 已有 1 条 history，页面 Loss 1 点，评估表两行在线 `surface_pressure`/`volume_velocity`；安装副本 `loop.py` sha1 `faaf245f1554cde46e7cc06c0c305f4de7aeb225`。共享外流案例「启用测试评估」只读关闭，本任务不能开测试评估列。

WDNO现行协议收尾（2026-09-17）：修复公共指标只跟踪清单、未跟踪实际数组的问题；新指标捕获全部数组依赖，历史索引不回写。当前四包隔离安装下，recipe、基础example、扩展各自从原数据8/2/2完成分阶段Task与直接运行，2→3完整状态/预测逐值一致；复制资产离开原位置仍可消费，Python/CLI固定目标中断恢复与不中断相同，历史2000→2001及16/16预测对照通过。90个唯一圈定用例通过；圈定WDNO task/recipe/extensions/public_metrics/task_assets/documents/metrics/reference_protocol/budget/audit、固定公开基线、公共recipe/Task执行及文档。正式入口与原180分钟累计预算见.context/mvp/wdno-acceptance.md；论文精度、超分/控制/Smoke未完成，主环境及8000/5173不变。

SafeDiffCon现行公共约定验收（2026-09-17）：模板与Burgers/Tokamak均为可复制完整目录，由pipeline.py/config.yaml发现，同一正文支持直接/Task；inputs.infer.solver_assets捕获KSTAR资源，解释器保留venv路径；旧配置显式转换为新副本，结果登记自包含数组目录及阶段权重。实际隔离安装下原数据8/4/2、2→3训练恢复、两轮后训练、适配与响应双入口逐值一致；历史5000/8000各续训一步及旧固定结果CLI post通过。正式主环境和8000/5173不变，未达论文精度。圈定SafeDiffCon conventions/task/task_replay/integration/packaging/budget/reference、公共recipe/migration/metric_indexes/array_bundles、Task configuration/management/contracts/execution、固定公开基线与文档；安装入口、最终测试和原累计预算见.context/mvp/safediffcon-acceptance.md。

Recipe/Task公共约定调整（2026-09-17，实施中）：官方模板由pipeline.py/config.yaml发现，公共输入为inputs.<stage>.<name>，run_root/data_root分离；Python决定步骤顺序。Task不再读取task-entry.json或自动补旧操作，缺组件操作只使对应管理功能不可用。公共资产和指标索引由writer交付；资产依赖变更不可用，指标缺科学口径不可比。旧任务迁移保留原件与冻结产物；已核验完整摘要的官方旧包装可在平台检查/提交时按当前数据集、模型及变体选择对应案例升级，写入失败恢复原件。自定义或未知正文走显式候选离线迁移，禁止凭导入/函数名覆盖；各领域原流程必须保留。2026-09-17用户授权后，正式8000已成套重装并重启；5173继续读源码。同日按用户要求清理正式工作区：只留首页项目 `7142b2dc062e4808a1686795076b8028` 与任务 `9c1dac251856411d9091d8d1cc60bc3e`；删除该任务全部旧准备及其余项目登记，abc 项目目录已删，真实 CFD 仅从正式 sqlite 注销、原文件未动。平台共享 shapenet_car/2/3 已从 abc 复制进首页并改消费键 `inputs.trainprep.dataset`，既有 shapenet_car4 同步改键；任务配置已解开准备绑定。ShapeNet/NASA 原数据根未删。首页任务已转公共 inputs，pipeline 含 rawprep/trainprep/train/infer/post，脚本换成现行 `recipes/aero_cfd`；原件留在 dojo_train 备份。平台检查只替换核验摘要的旧包装，原始处理写 `inputs.rawprep.source`，保存后刷新任务入口，失败空日志展示 run.error。数据准备目录里空的共享名不再报「已绑定来源不可用」。同日收口复核已重装 task/server 并重启正式 8000。整体计划尚未收口。现行证据见.context/mvp/recipe-task-conventions-acceptance.md；下文旧入口描述按历史范围保留。圈定test_recipe_conventions、test_recipe_migration、test_task_convention_execution、test_task_contracts、test_task_configuration、Task推理/平台配置/数据绑定、GenCP安装/固定公开基线和浏览器stage-consistency、execution-log。

WDNO近期计划实施（2026-09-17）：真实隔离安装后公开API新建Task，原数据8/2/2整轨迹分次处理、准备、训练、推理、post和续训2→3成功；直接/Task训练状态及预测一致，缺输入拒绝、固定post无模型成功、版本不增加。新增完整扩展覆盖集和能量读回，独立Agent质量误差变体实跑及独立数组复核通过。圈定test_wdno_{task,recipe,extensions,agent_acceptance,budget,audit,metrics,reference_protocol}.py、固定公开基线、公共recipe和文档；55唯一用例最终通过，详见.context/mvp/wdno-acceptance.md。原180分钟累计109.8634分钟。仅基础使用补验，论文/超分/控制/Smoke仍未完成，主环境和正式服务不变。

平台改走现行准备与模型架构（2026-09-17）：开训、公开检查、结构跟踪与推理只消费 version=2 准备和现行 `fitting`。官方外流任务里仍 import `train.physical` / `trainprep.physical` 的冻结脚本，在检查或提交前替换为现行 `recipes/aero_cfd` 同名文件；用户加过顶层函数的脚本不覆盖。配置读写与开训合成把 `trainprep.normalization` 每场写成显式 `scale`（AB-UPT 统一空间缺省 1000，其余 1）。version=1 准备不能改写成 v2，选中即拒绝并要求按现行数据准备重新生成。圈定 `test_web_stage_consistency.py`、`test_algorithm_platform_contract.py`、`test_task_configuration.py`。隔离 `7999`/`5172` 已冒烟：v1 开训 400 且页面为「须按现行数据准备重新生成」，不是「声明已变化」；v2 开训 200 已提交（隔离假数据随后可失败）。当时隔离入口已核对；正式 8000/5173 已在同日 Recipe/Task 公共约定更新中重装重启。

WDNO方案归一（2026-09-17，仅文档）：基础预测迁移和公共Task/recipe适配已完成；论文精度、超分/控制/Smoke及独立Agent使用仍未完成。现行补全方案为 `.cursor/plans/wdno-reproduction-and-agent-composition.plan.md`，整体替换早期未实现描述；历史报告不改写。原180分钟账本累计106.7682分钟，后续读取实际余额，不重置。圈定 `test_architecture_alignment_documents.py` 与 `test_recipe_documents.py::test_authoring_rule_and_development_entry`，本轮不新增训练。

WDNO Task公共约定调整（2026-09-17，验收完成）：新模板由pipeline.py/config.yaml自动发现，inputs按阶段声明路径，data_root与TrainingRun.output_dir分配本次数据。两种入口共用正文，数组资产和MSE口径用公共索引；不新增task-entry或Task模型执行器。旧配置显式转换为新副本，旧准备/权重按原科学合同消费。圈定test_wdno_{task,recipe,migration}.py、test_recipe_conventions.py、固定公开基线及文档；原数据双入口2→3及历史2000→2001续训/预测逐值一致，52项圈定测试通过；证据与累计预算见.context/mvp/wdno-acceptance.md，沿用原180分钟账本。

WDNO 缩小范围迁移（2026-09-17，基础切片验收完成）：原180分钟账本累计；同一Python3.12/Torch2.14环境原Trainer与Dojo各2000更新，完整权重/EMA/优化器/损失及64/128条预测逐值一致，测试MSE0.060287。Dojo实际2→50→2000恢复，原权重导入重放通过。新增spatiotemporal_pde领域、WDNO贡献能力和可复制recipe；共享数组清单/迭代装配/EMA保留旧入口。圈定test_wdno_{migration,recipe,migration_acceptance,audit,reference_protocol,budget,metrics,slice_acceptance}.py、SafeDiffCon/GenCP兼容、固定公开基线及文档测试；78项圈定测试通过，累计耗时见.context/mvp/wdno-acceptance.md。只发布隔离Python入口，不更新主环境或8000/5173；论文精度与超分/控制/Smoke未完成。

WDNO 原仓库切片（2026-09-17，P0/P1完成）：冻结原源码/补丁、代表轨迹名单和三小时累计预算，原Trainer与原采样在既有隔离环境执行；主环境和正式服务不变。只验证基础预测切片，未接入Dojo模型、未通过论文精度，精确数据流恢复属于后续迁移。圈定test_wdno_{reference_protocol,budget,metrics,slice_acceptance}.py及文档导航测试；2000更新和64/128固定评价完成，累计31.57分钟，测试MSE0.04938，独立主检查点重放逐值一致，16项圈定测试通过；实际证据以.context/mvp/wdno-acceptance.md为准。

训练设置开训与运行监控（2026-09-16）：训练设置页选择已准备完成的数据集、重新开始或继续训练（继续再选检查点）后开始训练，成功跳到训练运行。运行页只监控状态、曲线、日志和资源，可查看运行和停止，不再选数据或开训。继续训练只覆盖这一轮起始权重，不写进已保存配置。开训按当次平台选择合成运行配置，清单从所选准备生成，不夹带其他页留下的物理清单。圈定 `test_web_stage_consistency.py` 与 `e2e/stage-consistency.spec.ts`、`e2e/execution-monitor.spec.ts`。

训练设置预检与步骤完成态（2026-09-16）：训练设置预检只核验本页参数，不要求 `train.preparation`。点保存即记下完成，未改参数也会落盘并盖住旧失败预检；跳步、导航和刷新按已保存/已检查事实恢复绿色勾，不因全局修订清掉。完成态只在同一页参数变化时失效。圈定 `test_web_stage_consistency.py` 与 `e2e/stage-consistency.spec.ts`。

模型/训练页官方组合（2026-09-16）：模型设置与训练参数在模型选择之下提供同一套官方「数据集-模型」名称；名单按当前模型列出全部案例，不按任务数据集过滤。选完只覆盖当前页，两页独立，不清权重、准备或另一页字段。圈定 `test_web_stage_consistency.py` 与 `e2e/model-picker.spec.ts`、`e2e/stage-consistency.spec.ts`。

数据转换与准备组合（2026-09-16）：数据准备按当前数据集列出五份官方数据集-模型组合并加载该案例处理声明；模型不同则走换模并解除旧准备。平台「数据转换」每场有归一化方法和可见 scale，缺省 1；统一空间只映射到 [0,1]，附加放大不再写进坐标方法。AB-UPT 官方案例坐标预填 1000，Transolver-3 为 1。旧冻结不再按方法内 1000 解读，须按新声明重做准备。圈定 `test_algorithm_platform_contract.py`、`test_web_stage_consistency.py`、`test_train_reference_stats.py`、`test_web_recipe_compatibility.py` 与 `e2e/stage-consistency.spec.ts`。

平台执行配置（2026-09-16）：四页通过平台阶段模块合成完整配置，明确区分赋值、删除、空值与未编辑；平台值优先，未编辑研究参数保留。Task 的 replace_configuration 只按修订完整保存，不递归合并或解释算法；原 save_configuration 兼容。检查与提交消费保存修订，配置覆盖不授权物理数据覆盖；算法、contrib和recipe不新增平台规则。验收入口 `.context/mvp/platform-configuration-acceptance.md`，测试 `test_web_configuration_composition.py`、`test_web_configuration_handoff.py`、`test_task_configuration.py` 及 `configuration-composition.spec.ts`；未完成边界以验收记录为准。

项目共享数据（2026-09-16，本轮实施）：Task 正式原始处理默认写 `shared/datasets/<name>/content`，运行报告留任务 runs，准备副本与预测留任务 data。同名无显式覆盖则失败，`submit_run(overwrite=True)` / CLI `--overwrite` 只授权本次覆盖；不保留物理修订、不改已有任务，新准备读取同名当前数据，旧冻结消费仍校验。Task 独立发布，平台只登记跨项目引用。正式共享名称必填，试跑/检查不发布。旧入口显式适配不回写冻结脚本；旧任务产物迁移只复制新副本。旧登记名与冻结配置名不一致时按明确名称和正式运行配对迁移，不替换为最新同名运行。当前进度与证据见 `.context/mvp/task-shared-datasets-acceptance.md`，以下历史规则按当时范围保留。

正式模型结构显示（2026-09-17）：生成与载入过程在可视化区显示明确加载状态。TorchVista 跟踪按中等体量模块图发布：压缩视图、一层模块折叠，AB-UPT 域块走模块入口以便收成盒，不再默认展开整图算子。发布与页面载入仍按宽度适配初始缩放。圈定 `test_algorithm_platform_contract.py::test_real_torchvista_forward`、`test_trace_keeps_domain_blocks_as_medium_module_graph`、`test_trace_official_abupt_stays_medium_module_graph`、`test_fit_graph_viewport_uses_width_not_entire_graph` 与 `e2e/model-inspection.spec.ts`。

推理结果视图配置（2026-09-15）：表格弹窗提供Checkpoint/样本两模式，Checkpoint聚合全部分片样本并多选聚合，样本模式选一个权重、保留分片、不聚合。图表弹窗X轴跟随模式、Y轴多选表格字段；两处草稿独立，取消不生效。表格/曲线/导出均消费固定结果，不重跑推理。专项验收入口 `.context/mvp/inference-result-views-acceptance.md`。

训练运行与数据准备分离（2026-09-15）：开训在训练设置提交，只消费已准备完成的数据，不再选物理清单、不再「准备并训练」。服务拒绝 `prepare_first`，缺 `train.preparation` 不能提交；有正式准备未选择时主按钮不可用，不自动勾最新。数据准备仍是独立一步。训练运行只监控。圈定 `test_web_stage_consistency.py` 与 `e2e/stage-consistency.spec.ts`、`e2e/execution-monitor.spec.ts`。

推理工作台升级（2026-09-15，两道专项验收通过）：跨分片选择按检查点×分片串行，权重单次固定；步骤状态以完整批次为准。物理量分量评价且保留完整向量；有效实体mask、轻量指标、九项评价、样本等权统计与CSV/XLSX贯通。新post只读固定结果，旧post导入兼容，模板摘要仅作来源记录。1672×941关键区域最大误差2px（门槛4px），1440/1920、2000样本及状态截图通过；真实CFD两权重三分片、只评价/取消/中断/部分失败重试、固定数组重算、下载读回和Trame经正式8000/5173入口验收。CPU小模型2/1轮仅验证工程交接，不代表生产精度。文件—测试映射、实跑证据、两个未启用的真实换模用例及GPU范围见 `.context/mvp/inference-ui-acceptance.md`。

平台数据集与数据准备页对齐（2026-09-15）：原始处理执行前填写 `dataset.processed_name`，页面预填来源标识不算已写入，检查/试跑/正式执行前才写入配置；正式成功由 Task 在项目 shared 发布物理数据，平台按来源项目与共享身份登记清单、摘要；同名重做总需本次覆盖许可，页面询问后带 `overwrite_processed_name` 透传 Task 的真实物理覆盖；旧任务路径登记继续兼容读取；路径丢失或摘要变化标不可用；声明按规范化身份比较（不含 `rawprep.workers`、列表顺序和展开默认值），检查与登记共用展开后的 rawprep；读取/保存回传 `processed_name_status`，已存在时写明 VTKHDF 等差异并提示覆盖，不把并行线程写成原因。数据准备按平台名称选择并写入既有 `train.manifest`，同一清单只列一条且平台名称优先，不与任务历史重复可选；下拉按登记时间倒序，不自动勾最新；左栏复用产物树，右栏进度与叠加日志与原始处理同一套。原始处理不按 train/test 分片，只处理全部或指定样本；划分由数据准备完成。执行配置可改并行线程，写入 `rawprep.workers`，缺省 1、范围 1–64，每个线程处理不同样本；旧任务脚本未把该键列入允许名单时由官方任务显式配置适配器单独校验并写回，不改写冻结 `configuration.py`。执行配置四个按钮点下后立刻带动同一条进度、日志状态和说明；操作说明只在开始时打一行普通日志，不钉在底部。执行日志默认只显示最近 500 行，更早行留在内存并向上滚动展开，自动滚动开关必须能停住或钉住最新行。PT/Zarr 可同时写出，旧 `rawprep.format` 单格式仍有效；平台已提供的格式选项覆盖清单默认和旧单键，生成配置两键并存时以页面 `formats` 为准，不向用户报冲突；空列表或非法格式仍拒绝；VTKHDF 仅能力允许时显示，三个 ShapeNet 官方案例写 `rawprep.vtkhdf: true` 故新任务默认勾选，NASA 未接入不强开，已保存关闭不自动改写。归一化方法与统一空间分列，旧 `method: coordinate` 读成最小最大+统一空间且写出仍用原方法名。准备页可重划 train/test/eval：样本池为当前清单已处理的全部样本，默认显示原分片数量（缺的为 0），三者之和须等于总数，训练至少 1 个，test/eval 可为 0；抽取为保持原划分或随机，种子写入准备记录。官方 `partition.yaml` 原样保留，不重算张量；新划分是这次准备产物。无 `trainprep.split` 时保持物理清单原划分，只要求 train 非空，评估分片可空。圈定 `test_web_processed_datasets.py`（含倒序、缺时间回退、同清单去重）、`test_web_dataset_binding.py`、`test_web_rawprep.py`、`test_web_rawprep_handoff.py`、`test_web_stage_consistency.py`、`test_web_project_task.py`、`test_aero_cfd_documents.py`、`test_dataset_rawprep_descriptor.py`、`test_rawprep_manifest_configuration.py`、`test_rawprep_workers.py`、`test_recipe_configuration.py`、`test_train_recipe.py`、`test_trainprep_split.py`、`test_task_contracts.py` 与 `e2e/rawprep-consistency.spec.ts`、`e2e/execution-log.spec.ts`、`e2e/execution-monitor.spec.ts`、`e2e/stage-consistency.spec.ts`、`e2e/rawprep.spec.ts`。真实大数据交接仍按现有 `DOJO_*` 环境显式跑，缺数据 skip 不算平台资源验收通过。

文件树按需加载（2026-09-15）：原始数据、数据准备和后处理结果文件共用同一棵树表。首次只列当前一层，点开再取子项；点开文件夹只展开，不标选中。列举不哈希、不登记，预览/下载/加入三维时才按受控路径登记。打开原始处理只解析样本名单和代表字段，不给全部来源做内容摘要；同修订案例描述在步骤间复用。后处理指标目录与文件树分开，默认指标页不拉结果树。原始处理「处理结果」可选择已登记平台数据集，按登记时间倒序，不自动勾最新；未点选时本任务正式运行仍可浏览。圈定 `test_web_stage_consistency.py`、`test_web_post_results.py`、`test_task_post_results.py`、`test_task_post_metrics.py`、`test_web_dataset_binding.py` 与 `e2e/stage-files.spec.ts`、`post-files.spec.ts`、`post-ui.spec.ts`、`post-workspace.spec.ts`、`rawprep-consistency.spec.ts`。

后处理三页签切片（2026-09-15）：默认指标、结果文件、三维物理场可视化。指标从固定预测/真值重算，core eval负责数值、post负责绑定，task管理独立评价运行，writer独占运行报告；评价数据和显式导出在任务data/post。文件树复用files公开组件，按层打开。三维会话按任务持有，Tab切换只隐藏，追加按固定来源去重，离开页面才回收；刷新仍需保存配置重开。轨道回写相机不重读网格、不整屏刷新；命名块按路径与修改时间缓存。首次创建切面同步平面手柄；连点只留一份未应用草稿，不嵌套、不重复推送。对象树显隐只改当前对象/视图已有 actor，不重建映射；父子独立，来源行无总开关。真实状态与 Web iframe 响应回归见 `test_phys_display_updates.py` 与 `e2e/trame-responsiveness.spec.ts`。不重跑推理、不新增任务版本。当前验收范围见 `.context/mvp/post-workspace-acceptance.md`。

工作台步骤色与清单交接（2026-09-15）：已完成且非当前步骤用任务成功绿，当前步骤保持蓝；案例模板清单占位不当成已绑定失效来源，正式产物可落在项目、任务目录或已登记数据根；有正式清单未选择时提示先选且主执行不可用，不自动勾最新；摘要未返回前不闪「未运行」；原始处理无正式运行时不显示「处理中」；数据准备执行日志与原始处理同一套完整面板。圈定 `test_web_stage_consistency.py`、`test_web_project_task.py`、`test_task_contracts.py` 与 `e2e/stage-consistency.spec.ts`、`task-dataset-binding.spec.ts`。

独立推理实施完成（2026-09-14）：原子能力保留 `abilities/inference`，业务步骤位于 `applications/aero_cfd/infer`，外流新 recipe 显式串联 train → infer → post；新 post 只消费固定推理结果，旧 post 公开导入保留兼容，平台入口按声明调用而非模板结构比对。Task 固定checkpoint完整字节，同任务推理串行，输出各归子运行/数据目录，不创建任务版本。Web九步使用稳定slug，旧数字6→post、7→report保持。CORE38项、任务/服务/契约18项、旧profile11项、实际wheel和真实CPU 2份权重×2辆CFD车的下载/Trame交接通过；正式8000已更新，5173实际入口已核验。CPU小模型训练预算1样本、2/1轮，不代表生产精度；完整分项证据和跳过边界见 `.context/mvp/inference-acceptance.md`。以下历史验收保留当时范围。

模型目录与导出（2026-09-15）：模型页只列 AB-UPT 与 Transolver-3；换官方模型按当前数据集套对应 example 默认值，ShapeNet 的 Transolver-3 另选表面/体场。导出只存项目内同数据集配置预设，不含权重。生成结构由后台取最近相容准备或正式清单，现行 version=2 准备按训练同一条消费链跟踪，模型页不再勾这两项，不回写训练绑定。新建任务案例名为「模型 · 数据集」，含体场起步项。模型采样跟随当前模型自己的字段：AB-UPT 为点/锚点/查询，Transolver-3 为种子/步长/分块/随机流，步长只读；切片数量在主干参数。损失不可配时仍展示固定 MSE 与目标行。换模整段替换，无采样键才隐藏。模型设置不展示权重加载。圈定 `test_web_stage_consistency.py`、`test_algorithm_platform_contract.py::test_model_sampling_capability_follows_component`、`test_task_configuration.py`、`test_web_dataset_binding.py`、`test_web_project_task.py`、`test_web_recipe_compatibility.py` 与 `model-picker`/`stage-consistency`/`task-dataset-binding`/`model-inspection` 浏览器用例。真实换模仍用 `DOJO_MODEL_PICKER_REAL=1`，证据见 `.context/mvp/model-picker-acceptance.md`。

PI-BSNet 原案例迁移（2026-09-14，Neumann/Advection完整验收通过）：保留原参数导数、初始化与损失算术；实际Dojo分别5000轮/5000更新、2000轮/200000更新，平均相对L2为1.881%和10.427%。与同环境原函数全部权重、每轮损失和40个测试场逐值一致。75项圈定测试通过，含完整产物、恢复和真实wheel安装；梯形保留十实例，另外两例原算法切换范围待用户确认。入口tools/verification/pibsnet/source_dojo.py，报告工具source_migration_report.py，证据见.context/mvp/pibsnet-acceptance.md。

PI-BSNet Neumann/Advection 独立六组测试（2026-09-14，完整预算完成）：用户批准同时测试源码与物理导数解释，Neumann交叉整轮/逐实例更新；完整预算后再决定迁移，本轮不改Dojo算法。入口 `tools/verification/pibsnet/neumann_advection_trials.py`；圈定 `test_pibsnet_neumann_advection_trials.py`，进度与未决项见 `.context/mvp/pibsnet-acceptance.md`。

PI-BSNet梯形迁移（2026-09-14）：用户选定10实例、100×20×20、3000轮/PDE权重0.001，保留原Euler与参数样条数值行为。唯一梯形案例替换旧完整映射变体；旧数据/准备/检查点拒绝续用。Neumann/Advection本轮只审查，冲突须先确认。圈定 `test_pibsnet_trapezoid_alignment.py`、`test_pibsnet_trapezoid_acceptance.py`、生成/数值/训练/准备/安装/文档和post相关用例；正式结果见 `.context/mvp/pibsnet-acceptance.md`，不以单步对齐代替完整精度。

历史 Web / Server 首期切片（被后续并行扩展范围取代）：React/TypeScript/Vite/Ant Design 与 FastAPI 已按 ADR 0004 初始化。仅原始处理开放真实执行，服务通过 task 原 submit_run 使用既有 recipe；该历史切片未改 recipe/core/contrib；当前批准范围以以下并行实施段为准。task 增加配置与管理公开操作；创建模板允许未绑定输入，执行仍严格检查。viz 增加独立文件预览进程，与原有静态比较共存。实际验收与未交付边界见 `.context/mvp/web-rawprep-acceptance.md`，不能用工程构建替代端到端验收。

整合交互原型：`docs/prototypes/dojo-web-integrated.html`。沿用旧平台框架，内嵌第 2–7 步独立页面；项目页与第 1、8 步保留。单文件离线可打开，阶段配置按任务保留于当前会话，刷新重置；无真实计算或后端交接。验收：`tests/integration/web_integrated_browser.cjs`。

交叉模型实施（2026-09-10）：唯一 aero_cfd recipe 的五个独立 example 使用共享物理工作流；物理 PT、模型准备和完整预测分开交接。NASA 无体场，单表面 AB-UPT 配置不能含跨域块。本期五组正式单轮实跑、固定五样本全点评价及报告已交付；范围与工具限制见 `.context/mvp/cross-model-acceptance.md`。圈定新增用例：test_physical_dataset_contract.py、test_model_preparation_contract.py、test_cross_model_recipe.py、test_cross_model_training.py、test_cross_model_comparison.py、test_comparison_visualization.py、test_cross_model_acceptance.py；同时覆盖原数据、模型、训练与 post 相关回归。viz 只依赖 spec，比较数据独立于运行目录。

SafeDiffCon 三小时集成（2026-09-16，续接缩小验收完成）：保留4000步历史基线，补独立原定义的后训练/适配参考、原Trainer恢复、冻结配置与阶段限时；预训练总目标默认4000、配置上限20000，仍受每案例原版与Dojo、校准、后训练、适配、响应、失败重试合计180分钟原账本约束。控制领域与可复制recipe已接入，平台不扩展。双方共享准备数组、批次流、状态容器、响应求解与指标存储，不能称零共享完整论文程序。圈定test_safediffcon_{admission,reference,integration,packaging,budget}.py、test_gencp_abilities.py、test_public_api_stability.py与test_architecture_alignment_documents.py；两例5000/8000步与各50样本对照逐值一致，66项测试及最终安装重放通过；累计104.79/57.25分钟，未达论文精度。实际执行与最新成绩见.context/mvp/safediffcon-acceptance.md。Tokamak两类目标与论文离散归约未决，工程一致不等于论文精度。主环境与正式服务不变。

SafeDiffCon Task接入补验（2026-09-17）：此前模板缺task-entry.json，Task提交被拒绝；新增task.py与阶段输入/输出声明。两例经真实Task新建、训练、后训练、推理及固定结果post通过，各新增2次预训练、两轮各2次后训练、2样本响应，只验证交接。缺权重失败、禁用模型/求解器后post成功，版本不增加；37项测试及Burgers CLI post通过。圈定test_safediffcon_task.py、test_safediffcon_integration.py、test_safediffcon_packaging.py、test_task_contracts.py、test_task_management.py；证据见.context/mvp/safediffcon-acceptance.md。普通Task API/CLI可用，不代表Web或专用推理批次接入；主环境和正式服务未改动。

