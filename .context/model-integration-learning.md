# 模型集成改进日志

本日志为[集成 skill](../.agents/skills/dojo-integrate-model/SKILL.md)提供事实反馈，不替代实验验收，也不自动增加规则。阶段交付简短追加即可，无固定模板；无新发现时注明无新增，不编造经验。

记录实际现象及证据、处理结果、适用范围、单次或重复验证，并区分：

- **已证实的错误边界：**什么判断或做法造成错误，在哪些条件下必须避免。
- **可泛化的流程候选：**什么做法实际帮助定位或解决问题，适用条件是什么。
- **待核实观察：**尚无充分证据的推测，不作为规则依据。

已核实错误仍按仓库约定进入 [error.log](../error.log)，这里引用即可。更新 skill 时只提炼通用结论，保留三部分结构；单次经验不自动升级为通用禁令。

## 2026-09-16：从已有集成实跑提取

以下来自已有实跑记录的复核，本次修改文档未重新训练。

- **已证实的错误边界｜内部通过不等于用户入口可用。** 分场训练曾因未核对 writer 标签约定而在保存时失败；recipe 重导出又曾被格式规则删除，造成入口失效。分别以 namespace 和显式导出修复，并补复制入口验证。依据：[原始错误记录](../error.log)的当日 GenCP 条目、[安装验证](../tests/integration/test_gencp_installation.py)。两个不同故障支持核查上下游交接；具体标签和导出写法不能推广为所有模型的要求。
- **已证实的错误边界｜不能用放宽容差掩盖配置遗漏。** 单场生成参数遗漏造成约 0.03 的预测差，恢复参考设置后对齐，未放宽容差。依据：同一[错误记录](../error.log)。这是一次故障及修复验证；可推广的是核对实际生效参数，原步数仅适用于原实验。
- **可泛化的流程候选｜先确认比较对象所处阶段。** 原始真值与归一化后再反变换的参考目标有舍入差，后续分别比较原始真值与参考评价。依据：同一[错误记录](../error.log)。单次验证；提醒区分处理阶段与精度，不能据此将未知偏差一概解释为舍入。
- **可泛化的流程候选｜按影响范围补验并保留累计成本。** 已完成训练的 Turek CNO 从完整检查点恢复，执行零次新更新，补验证、生成和 post；原训练与恢复均计账。依据：[验收记录](mvp/gencp-acceptance.md)的恢复说明及其指向的 budget.json。一次完整恢复实跑；仅在训练语义、检查点和消费契约仍兼容时可复用，不能据此跳过受影响的训练。

## 2026-09-16：SafeDiffCon 原方案续接计划复核

本次仅按用户提供的原方案和现行 rules 更新[实施计划](../.cursor/plans/safediffcon-integration.plan.md)，复核现有源码、[验收记录](mvp/safediffcon-acceptance.md)与累计账本；未启动训练，无新增实跑结论。计划保留原 A—E 功能，取消完整训练另计的旧安排，区分已有共享编排对照与待补独立参考；这些是本案例范围说明，不上升为新的通用规则。

## 2026-09-16：SafeDiffCon 续接实际实施

- **已证实的错误边界｜放开执行预算还要核对配置门禁。** 首次恢复诊断被旧4000步配置上限拒绝；修改总目标校验并增加整数/上限测试后，两案例4000→4100双侧恢复的历史与参数逐值一致。依据：[错误记录](../error.log)、[专项验收](mvp/safediffcon-acceptance.md)及其续接恢复证据。两案例实际验证；只改变配置允许的总目标，不重置学习率、随机流或累计时间。
- **可泛化的流程候选｜独立性按阶段说明。** 从冻结源码提取原校准类、训练步与适配步，独立编排后，两案例小样本诊断的权重和控制逐值一致；共享数组、批次流、容器、求解和指标仍显式列出。依据：续接目录的两个diagnostic-acceptance记录。此法验证了这些原定义的迁移，不代表未经修改的整套原程序或论文设置。
- **已证实的错误边界｜短测不能保证持续吞吐。** Burgers100步诊断约0.42秒/更新，持续参考续训变慢至约1秒以上；原7000步尝试及时停止并计账，按耗时改为5000。尚未启动的Tokamak也保守收紧为8000，未参考最终测试成绩。依据：冻结与superseded记录、执行日志和原累计账本。一次设备负载下的观察，不能据此推广固定的减速倍率。

- **待核实观察｜短训后的安全重权可能失效。** 本轮Burgers两轮保存Q对应的原指数权重全部下溢，按原定义回退等权；两侧完整控制响应仍逐值一致，但指标较基线退步。Tokamak同样两轮的权重均非零，指标有所改善。依据：两个reweight-diagnostic及final-acceptance记录。数值状态已实测；是否足以解释精度差距、何种算法修正可改善仍未验证，不将相关性当因果，也未用测试集挑选修正。

## 2026-09-17：WDNO 按最新集成更新计划

本次仅更新[WDNO 计划](../.cursor/plans/wdno-reproduction-and-agent-composition.plan.md)，读取当前 GenCP/SafeDiffCon 源码与验收记录，重新计算基础训练/测试文件摘要（与已有审计一致）；未新跑 WDNO 训练，也未重新验收其它模型。

- **已证实的错误边界｜旧缺项清单已不符合当前代码。** 旧计划拟新建按步循环并扩建 EMA；当前 iterations/checkpoint 已存在，SafeDiffCon 已有保留原计数的 EMA 和尾批流。计划改为复用并核验更新顺序：现有循环直接传 EMA 时先于 scheduler，WDNO 需采用已有 after_update 连接。依据：计划“当前源码依据”。本次为静态源码核对，尚无 WDNO 数值对齐实跑。
- **可泛化的流程候选｜按实际语义区分复用范围。** 控制准备要求 case/outputs，GenCP 为流匹配与耦合场，不能因同属生成式 PDE 就认定 WDNO 小波准备和噪声损失可直接复用。保留领域交接及专用数值缺项，不重建已有训练底座。当前为单次计划复核，实际接入收益待验证。
- **待核实观察｜三小时持续吞吐。** 沿用 9 月 16 日 WDNO 短测速，参考 SafeDiffCon 已记录的短测/持续耗时差异，保留累计截止和评价预留；未推定固定减速倍率。三小时原版切片及论文复现仍未完成，其它模型的缩小预算授权不迁移到本案例。

## 2026-09-17：GenCP Task 真实入口复核

- **已证实的错误边界｜一种运行入口的成功不能证明另一种入口可用。** 历史安装测试直接运行 recipe；本次六组经 Task 新建后，训练/推理/post 十八次提交均因缺入口声明而在启动前失败。依据：[Task 补验](mvp/gencp-acceptance.md)及其复核报告。六组重复验证同一缺项，未验证 Task 内数值计算。适用边界是交付包含该入口时才必须验它，不把 Task 或平台接入自动加入所有模型任务。
- **可泛化的流程候选｜从用户实际调用入口逐层核验。** 本次先调用公开新建/提交接口，再核对已安装关键模块与源码一致，确定是模板交接缺失；在入口修复前没有重复训练。仅一次调查流程的证据，不规定固定工具或统一排查顺序。本轮保留为日志候选，不改 skill 正文。


## 2026-09-17：SafeDiffCon Task实际入口补验

- **已证实的错误边界｜脚本/wheel对照不覆盖Task托管。** 原模板没有task-entry.json，真实提交拒绝；补显式入口和阶段交接后，两例Task训练、后训练、推理和post通过。证据：task-verification下两例acceptance/runs，见[专项记录](mvp/safediffcon-acceptance.md)。两个案例的工程短运行，不是论文精度再验收。
- **可泛化的流程候选｜用不可用上游验证固定结果消费。** 将模型与求解器设为不可用后post成功且固定结果不变，缺推理权重则明确失败。两例支持检查阶段输入捕获边界，不能据此宣称所有模型支持Task专用批次操作。

## 2026-09-17：SafeDiffCon Task原因分析与优化计划

- **已证实的错误边界｜源码可用不等于执行子进程可导入。** 既有 `task-validation-20260917/before.json` 记录控制模块导入失败；与缺入口导致的提交拒绝是两类问题。历史运行未保存完整包来源，不能把安装漂移的具体原因当成已证实。计划补实际解释器和模块来源证据，尚未实施。
- **已证实的错误边界｜后处理隔离不替代正常训练验收。** 本轮只读两例 `*-complete` 报告及检查点，确认从零训练2次、恢复至3次、历史前缀和权重变化；不可用模型/求解器只出现在后续独立post。两例有限训练已成功，尚不能证明直接脚本与Task所有数值一致，更不是论文精度。本次没有新增训练。
- **可泛化的流程候选｜减少重复流程并验同输入结果。** 当前pipeline与task脚本维护两份正文，加载器有重设阶段后补回逻辑；[待实施计划](../.cursor/plans/safediffcon-task-transparent-execution.plan.md)拟合并正文、保留旧入口、补真实双入口对照。静态分析的改进候选，效果待实跑验证。

## 2026-09-17：GenCP Task 原因隔离

- **已证实的错误边界｜提交成功不能证明阶段选择被执行。** 隔离副本只补入口后，请求 train，但原 pipeline 的 only 参数覆盖为 pipeline，检查日志进入全部阶段；Task 状态仍成功。检查模式无训练。依据：[隔离诊断](/Users/zonghui/work/project_simulation/dojo_train/gencp/task_diagnosis_20260917/report.md)。单次验证；应核对请求和实际阶段，不把这种具体入口写法推广为所有模型问题。
- **可泛化的流程候选｜逐项补足交接，区分算法问题与执行外围问题。** 保持所有 Python 字节不变，只声明入口和路径，原 post 经 Task 成功且指标与直接运行一致；未声明数据输出的检查则保留外部路径。单次真实 post 和两次检查，不能推广为训练/推理已通过。方案待实施，未改变通用 skill。

## 2026-09-17：WDNO 原仓库切片实际执行

P0/P1已完成：原Trainer完整网络2000更新、64验证/128测试、独立原构造器检查点重放及固定结果复算；累计31.57分钟。测试MSE0.04938，16项相关测试通过；完整证据见[专项验收](mvp/wdno-acceptance.md)。这不是Dojo迁移或论文复现。

- **已证实的错误边界｜观察摘要不能替代完成状态。** 两次更新诊断的last_loss一度保留前一步值，完整历史正确；在主训练前改为结束时从历史末项取值，最终真实验收核对完成步数、历史长度与末损失。依据：error.log同日WDNO条目和实际切片测试。单次记录故障，未影响训练数学。
- **可泛化的流程候选｜落盘后用独立实例重放。** 主进程完成后，通过原构造器加载落盘普通权重，首批16测试预测逐值相同；随后固定数组对原始真值/名单复算，最大归约差5.96e-8。证明了本次权重与输出交接，不扩展为完整论文程序独立复现。
- **待核实观察｜短测与实际训练器吞吐差异。** 本次原Trainer完成2000更新及评价的累计耗时低于先前带额外同步的短循环估计；同时运行后段也比前段慢。保留原上限，没有据此扩大训练。同步方式、设备负载等原因未隔离，不能推广固定提速/降速倍率。
- **可泛化的流程候选｜先核对条件约束的位置。** 原源码把初态写入独立条件通道，重建初帧仍可能偏离真值。主测试初帧最大误差0.53238，按原定义从MSE中排除；没有为图像更好看而替换真值。该结论仅适用于本次源模型语义，不推广到其它条件模型。

## 2026-09-17：WDNO缩小迁移阶段验证

- **已证实的错误边界｜划分种子与模型种子不可混用。** 初始两个迁移更新误用42，原冻结训练协议为0；准备18000条已逐值一致，损失仍不一致。修正后2步及实际检查点恢复到50步全部损失逐值一致。当前参考工具逐项核对冻结训练参数；见error.log、`dojo-final/resume50-comparison.json`。单次实现错误，不更改原结果。
- **可泛化的流程候选｜同环境比较与旧环境基线分开。** 原Torch2.4.1切片保留，新Dojo依赖Torch2.14.0，因此另跑同环境原Trainer，而不将版本差异归为迁移算法误差。现已完成全部2000步及64/128固定预测逐值对齐；证据见专项验收，不推广为跨环境等价保证。
- **可泛化的流程候选｜从第二个真实使用点提取机制并保留旧入口。** WDNO与控制共用数组清单、完整迭代装配和原EMA适配，旧导入保留；SafeDiffCon/GenCP及固定公开接口基线通过。只支持本次共同需求，不建立统一模型协议。

- **已证实的错误边界｜交付摘要也要核对实际路径。** 真实数据变体已完成64/128预测与能量读回，但配置路径字段被结果遍历变量覆盖。保留修正前摘要，修正为实际config.yaml并增加交付断言；模型与数组不受影响。与上一阶段末损失摘要问题共同说明运行成功不能替代交付引用检查，但两次均为本例工具实现问题，不自动升为统一框架协议。

## 2026-09-17：WDNO基础迁移完整对照收尾

- **已证实的错误边界｜预测逐值一致与指标汇总逐值一致分开。** 全部预测最大差0，原版/Dojo汇总MSE仍有小于2.4e-10的浮点归约差；报告保留两份原数，不声称指标逐值一致。仅本次环境与切片验证。
- **可泛化的流程候选｜恢复跨越完整数据轮次。** 实际2→50→2000恢复跨过首轮1125批，全部损失和最终权重/EMA/优化器仍与连续原Trainer一致，补充了仅同轮恢复的证据。source原权重导入另做两组16条预测重放，不冒充精确恢复。
- **已证实的错误边界｜变体产物也需核对可重用配置路径。** 变体结果摘要被循环变量写成预测清单路径，已修正该元数据并保留原摘要；原数据小网络2更新及64/128预测、派生能量不变。见error.log与variant目录。单次工具错误。
- **待核实观察｜完整论文与独立Agent易用性。** 当前仅基础预测切片；代码提供替换点且实施者已实际运行变体，尚无陌生Agent现场验收，不把它列为已完成。

本次最终78项圈定回归全部通过，文档复查5项通过；原账本累计103.8907分钟且全部关闭。证据见专项验收与dojo-final/delivery.json；主环境和正式服务未改动。

## 2026-09-17：WDNO跟进公共Task/recipe约定

- **已证实错误边界｜源码存在不等于worker安装可用。** 同时进行的仓库更新曾令复制脚本引用application_parameters而安装副本没有该名称，另一次候选包缺显式转换函数；失败发生在训练前。保留首次测试输出，重新按当前公共入口构建隔离包，再做双进程实跑，不重装主环境。
- **可泛化流程候选｜路径调整与数值产物兼容分开。** 新用户配置拒绝旧树，显式转换生成副本；旧分片清单/训练合同保留原义。直接与Task按相同原数据各完成2步及恢复到3步，已逐值核对状态和预测；历史2000步补验另列，不能用配置解析替代。

本次最终52项全部通过；新旧安装各2000→2001更新的全部状态和各16条预测逐值一致，最终安装原数据双入口与source导入再次通过。累计106.7682分钟，历史证据不改写。仅本次WDNO公共约定迁移验证，不把某套输入命名自动提升为全仓强制协议。

## 2026-09-17：WDNO现行方案归一（仅文档）

- **已证实的错误边界｜计划正文与最新验收不能混读。** 旧计划开头和末节已记基础迁移、Task适配完成，中段仍写专属数据、权重导入和recipe“尚未实现”，阶段顺序也保留原先迁移前门槛，易造成“尚未集成”的错误结论。本轮核对当前源码、专项验收及task-conventions-v3/delivery.json后整体重写现行方案；历史实验报告不修改。单次文档冲突核查，未新增数值证据。
- **可泛化的流程候选｜按完成范围表达接续目标。** 分开基础工程迁移、同条件数值一致、论文精度和独立Agent使用；后续计划以已交付入口为起点，保留原累计预算，未运行的任务写成待验收。实际效果尚未经过独立Agent使用验证，不把本次方案整理称作使用验收通过。

## 2026-09-17：WDNO计划按rules修订

- **已证实的错误边界｜文档测试通过不代表计划编写规则全部满足。** 前版虽通过导航检查，讨论区仍含文件/执行安排，行为验收挤成一行，文件改动以宽泛目录代替具体文件，示例还需人工补全。本次按 `plan-business-alignment.mdc` 重排为业务总结、讨论、实施评审；补完整配置、流程、普通能力与新增输出读回示例，并细化文件及逐叶测试。仅静态文档核查，未新增训练或独立Agent证据。
- **用户补充验收要求。** Task须以公开API新建WDNO，分别实际训练、推理、后处理和续训，记录新的运行身份及产物，不能以提交成功或历史报告替代本次补测。该要求已纳入A2和S1b；最新请求先修订方案，实际补测仍待执行。

## 2026-09-17：WDNO近期计划实施与独立使用

- **已证实错误边界｜固定数组与评价报告是不同交接物。** 新Task补验把原float32逐样本指标误与float64全量均值作1e-12比较，随后误认新预测清单含mse。查现行消费链后，使用post.sample_mse与固定数组连接既有独立NumPy复算标准，实际分阶段和两入口状态通过；仅验收工具修正，算法/历史结果不变。
- **已证实错误边界｜父进程可导入不能替代真实worker环境。** 主环境没有小波依赖，首次相关回归3项worker失败；原隔离实跑已成功。改用WDNO隔离解释器及安装集合后通过，不重装主环境。失败和重试均计入原账本。
- **可泛化流程候选｜独立使用和独立产物复核分开。** 未参与实施Agent用公开说明自行增加M/L误差、从零/续训/独立post一次实跑成功；主Agent不执行其能力代码，直接对固定数组、身份、单位/轴和检查点复核。支持本次公开使用材料的有限验证，不推广为所有变体可用。
- **已证实错误边界｜并行架构改变会使文档断言过期。** 最终计算用例通过后，盘点测试仍要求已删除的旧task-entry文件；改核当前WDNO配置与流程文件后文档复验通过，未改固定公开代码基线。

证据见wdno-acceptance及task-stages-20260917交付；55个唯一用例最终通过，累计109.8634分钟。完整论文协议、高分数据和正式精度仍未闭合，计划后续阶段保持未完成。

- **已证实错误边界｜Task独立进程不能靠验收父进程退出收尾。** 当前Task以独立会话启动worker，补验工具改为在中断/等待超时调用公开停止接口；实际延迟worker超时后状态stopped，且正常分阶段训练回归通过。仅验证合作式停止及当前公共接口范围。

## 2026-09-17：WDNO 新建任务重复验证

- **重复验证，无新增错误边界。** 用户再次要求测试 Task 新建后的训练、推理和后处理。复用已交付隔离安装，使用当前复制 recipe 在全新项目完成六阶段运行、双入口数值对照、独立 post 与缺输入失败验证；新产物专项 1 项通过。证据见 `wdno-acceptance.md` 本日复测及 `task-recheck-20260917/REPORT.md`。仅支持同一 Burgers 小预算工程范围，不扩大论文、Web 或任意变体结论；累计账本续接至 110.2245 分钟。

## 2026-09-17 公共Recipe/Task迁移续接（实施中）

- **事实错误／不可沿用的方向：**统一入口时曾将同一模板正文覆盖不同案例，损坏专属数据处理。已恢复来源摘要核验过的原正文，再做路径迁移。边界是统一交接不等于统一领域流程；以后迁移先核对来源、消费者和持久化格式。
- **事实错误：**新增双入口验证工具漏传配置修订，真实原始处理/准备后失败；修正工具并保留失败记录及耗时，不能算模型失败或删除预算。
- **实际困难：**仅迁移训练入口不足以保证管理功能可用。批量推理的样本检查仍消费旧路径，随后又遇到“缺省键”和“显式null”的冻结声明比较。配置需沿准备、检查点、只读检查、执行和比较核查；迁移后的科学声明按原业务语义核对。
- **可泛化流程候选：**先固定总训练目标，再用本次执行片段验证恢复；分别比较状态、固定预测和后处理。运行成功不替代内容比较。离线迁移先在原件副本生成候选，核验漂移、注入切换失败并验证回滚，再考虑正式发布。
- **证据与边界：**见 `.context/mvp/recipe-task-conventions-acceptance.md` 及其测试日志。迁移、平台与安装整体仍在验收，不将候选自动升级为skill强制步骤，也不写入个案名称/数量限制。

### Recipe/Task公共约定续接的实测记录（2026-09-17）

- 事实错误：训练已保存冻结归一化，但锚点推理仍重新绑定统计；无外部统计的NASA准备成功后推理失败。改为训练/推理共用准备消费校验，批量推理2项及原训练/预测31项通过。边界：缺失或改变的准备不能靠重新拟合掩盖。
- 事实错误：只迁移主模板漏掉复制扩展的旧路径；真实字段、采样和自定义能力测试失败。扩展按同一公共输入和会话输出连接后12项通过。流程候选：公共配置改变时逐条追踪仓库外扩展和独立阶段，不只运行主流水线。
- 对照条件：两个CPU子进程线程数不同，Transolver参数出现约1e-9差异；冻结相同线程数后严格逐值一致。流程候选：超差先核对实际执行条件，不直接放宽门槛。
- 迁移故障：候选目录复制失败时若尚未写入进行中日志，会留下无法走正常回滚的半份目录。提前提交日志并注入复制中断后可恢复原件。边界：部分复制也属于迁移事务。
- 仍未解决：普通Task支持旧领域准备，不等于平台新版准备接口已经支持所有模型；不得用替换测试模型掩盖缺口。完整状态见公共约定验收。

### 迁移收尾的实际核验（2026-09-17）

- 已解决前条平台物理准备差异：为原逐样本流程增加有明确身份的新版冻结记录，检查与批量推理消费原模型输入能力；未把模型换成通用模板来通过验收。
- 事实错误：历史配置转换遗漏旧post的预测参数和权重。配置能够加载并不证明研究意图保留；补了参数/权重、阶段拆分和幂等测试。
- 验收工具错误：中途状态使用了公开writer不支持的标签，导致重复失败。改用已有namespace；原失败与耗时保留。工具默认遇首错停止，先修共同阻断再扩大样本。
- 可泛化流程候选：入口迁移先分清原件的实际领域流程，再做缩小副本执行和完整恢复状态比较；生产候选单独保留原预算，禁止把验证用小配置发布。证据见公共约定验收和九任务报告；这些仍是经验候选，不新增skill强制细节。


## 2026-09-17 SafeDiffCon 公共约定适配

- 已证实错误边界：对 `solver.python` 使用 `Path.resolve()` 会穿透虚拟环境符号链接，Tokamak worker因此缺NumPy；改为保留虚拟环境身份的绝对路径，新回归与真实KSTAR响应均通过。配置路径和执行环境身份不能一概处理。
- 已证实工具问题：公共指标比较返回 `status=available`，旧验收工具误读 `comparable`；非连续响应的派生能量在保存前后归约顺序导致约3.55e-15差异，扩展显式使用同一连续布局后严格逐值通过，未放宽算法容差。
- 本次验证：两个完整案例和模板经实际安装Task/直接入口真实训练，原分片8/4/2、2→3恢复、两轮后训练、适配及响应逐值相同；历史5000/8000权重各续训一次成功。失败重试均计原账本，论文精度未达成。
- 流程候选（单轮验证，不自动升级为规则）：安装来源核对需同时覆盖父进程与worker；科学状态对照只排除已单独核验的运行位置，不能删掉数据身份或随机流来求一致。证据见 `.context/mvp/safediffcon-acceptance.md` 及实验目录 `public-conventions`。

## 2026-09-17：WDNO现行协议收尾

- **已证实错误边界：**指标绑定清单不等于绑定其数组。修复前真实公共比较测试在修改预测数组后仍返回available；贡献应用补齐全部依赖后，清单与预测/真值/ID/能量变化或缺失均触发对应分片不可用。科学可比性仍只依赖目标、样本与口径，不把预测内容加入实验身份。修复前后JUnit保留，旧索引不回写。
- **可泛化流程候选（本轮验证）：**复制成功之后暂时隔离仅本次原产物，才实际证明复制品可独立消费；固定训练总目标后中断/恢复，比调大目标续训多覆盖公开恢复接口。当前原数据与测试夹具都验证通过，不推广到未测模型或任意外部进程。
- **重复验证与范围：**三个真实来源分别复制，文档中的配置示例直接执行；新旧安装历史检查点续训与预测一致。90个唯一相关用例通过，完整论文精度未完成。证据见 `mvp/wdno-acceptance.md` 本次收尾和 `protocol-update-20260917/REPORT.md`；无新增skill强制步骤。

## 2026-09-20：MeshGraphNets / CylinderFlow core-first 接入

- **已证实的错误边界：**新模型的通用图构造、变长收批、mask、噪声、rollout 和轨迹评价若直接放进 contrib，会违反当前新增的 core-first 归属规则；实现已将这些中立能力放入 `ai4e-core/abilities`，contrib 只保留 MeshGraphNet 网络和 CylinderFlow/模型连接。
- **可泛化流程候选：**判断能力归属先看中立输入输出、合成数据可测性、数据集/模型私有依赖和潜在复用，不按源码来源或当前使用者数量判断。该规则已同步架构正文、AGENTS、算法规则和模块索引。
- **已证实的错误边界：**第一版把节点归一化限定为速度二维、masked MSE 对分量取均值、`mse_H_steps` 取单独第 H 帧，均与锁定的 DeepMind `cfd_model.py/cfd_eval.py` 不一致。现已改为完整 11 维节点统计、分量平方和后按节点平均、预测帧 1..H 累计 MSE；结构版本提升并拒绝旧权重。证据见 `.context/mvp/meshgraphnet-acceptance.md`。
- **已证实的错误边界：**完整轨迹推理最初从原始第 0 帧开始，而 DeepMind 先经 `add_targets` 取 `1..T-2` 再以其中第 0 项作为 rollout 初态。现已把该专属窗口放回 contrib inference 连接，并由 Recipe 固定结果断言初态身份；否则 horizon 算法正确也会评价错一段物理时间。
- **已证实的错误边界：**仅提供 TFRecord frame reader、Recipe 却只加载 `.pt` 不算官方数据接入。现已增加中立 bytes feature 解析，并由 CylinderFlow adapter 按 meta 解码；官方 train 首条 13.57 MB 完整轨迹已解析为 600×1876×2，真实帧前向和 2 步 rollout 有限。
- **可泛化流程候选：**源码静态核对需沿数据协议、特征方向、归一化、噪声 mask、损失归约、rollout 边界和指标归约逐项落实为测试；网络 shape 或 import smoke 不能覆盖这些科学差异。本次为一次完整纠错证据，是否提升 skill 等待后续模型重复验证。
- **已验证范围：**最终圈定 23 项通过，含中立图/归一化/累计指标、TFRecord/轨迹解码、直接 Recipe、固定 post、Task 托管、检查点 fork 内容校验和 1→2 更新恢复；core/contrib 当前 wheel 已安装，并在仓库外复制 Recipe 完成五阶段运行。
- **待核实观察：**DeepMind TensorFlow 同权重逐值对照、完整数据分片、原实现完整预算、论文 RMSE 与误差条尚未闭合，不能据此宣称论文复现或 PhysicsNeMo 数值等价。

## 2026-09-20：MeshGraphNet 静态外流扩展

- **已证实的错误边界：**把通用 `aero_cfd` Recipe 全部切到新版物理准备会破坏旧锚点准备摘要与数值基线。最终以 `trainprep.topology` 选择公开门面：图案例走 VTKHDF 拓扑准备、静态图训练和完整拼回，原点模型继续走原准备、训练和锚点推理；阶段顺序与 Task 边界不变。
- **已证实的错误边界：**NASA manifest 标记 `vtkhdf: false` 会丢弃来源已有的 connectivity，使复制后的平台数据无法独立构图。现由 rawprep 用官方 connectivity 和样本坐标写 `surface.vtkhdf`，manifest 摘要覆盖 PT、VTKHDF 与身份资产；trainprep 不再读取原 HDF5/connectivity。
- **可泛化流程候选：**共享平台数据保存规范物理场和真实网格，模型派生的边、诱导子图、分区与 halo 留在准备缓存。这样不同图模型可复用同一 PT+VTKHDF 数据，而不把模型图结构发布成数据集字段。本次在混合单元、缓存删除重建、仓库外复制和两个 wheel 安装案例中验证一次，暂不推断所有网格模型均已适配。
- **实际验证与边界：**合成 ShapeNet 双域和 NASA 表面案例完成短训、完整拼回、五指标、固定 post、Task 执行/fork、提高目标轮次恢复及隔离 wheel 运行；ShapeNet 1→2 与连续两轮模型、优化器、调度状态逐值一致。后续在本机定位到真实来源，ShapeNet 单训练/测试样本及 NASA 两训练/一测试样本均完成平台 rawprep、短训和完整推理；NASA 454,404 个原 ID 无重复无遗漏。生产精度仍未验证，静态扩展也不属于 DeepMind 论文复现。
- **已证实的测试边界：**`warmup_cosine` 的首轮学习率由最终更新预算决定，把目标从一轮提高到两轮后不能与从头两轮逐值比较；恢复专项改用不依赖最终预算的 constant 调度，正式案例默认值不变，也未放宽张量门槛。最终 core/contrib wheel 在隔离 Python 3.12 中完成两个复制案例；源码覆盖测试与 wheel 安装验证分别记录，不能用源码导入替代安装证据。
- **已证实的真实数据边界：**NASA 新案例最初误用仅适合 VTK 目录来源的 `open_dataset` 流程，真实 HDF5 立即失败；改为现有显式物理 HDF 步骤。ShapeNet rawprep 已写 VTKHDF 但首版 manifest 未声明 `meshes`，导致 trainprep 不能按平台合同解析；通用保存结果补域到网格映射并增加实际 Recipe 回归。单一 NASA 工况的条件标准差为零，使用两个六维条件均有变化的正式训练样本后通过，未关闭标准化门禁。

## 2026-09-20：MeshGraphNet 静态外流正式发布与大图缓存复验

- **已证实的错误边界：**454,404 点图最初为每个核心块重复展开 1,819,400 条有向边，且 trainprep 没有持久保存分区；修复后单次构造邻接，按拓扑与预算缓存，并在 train/infer 相同预算间复用。真实复验产生 28 个核心块，核心 ID 无重叠覆盖全部节点，分区调用 1 次，首次 263.497 秒，缓存重载 0.029 秒。
- **已证实的交接边界：**平台 PT+VTKHDF、trainprep 图缓存、Python 模型运行和正式 Web 是四层证据。正式 5173/8000 已证明 149 样本、VTKHDF 保存重开和 `Sample002` 的 8 个 PT、身份、454,404 点 VTKHDF；Server 未登记 MeshGraphNet，不能据 rawprep 页面通过宣称 Web 已执行模型训练或推理。
- **已证实的测试边界：**公共配置已经拒绝 `dataset.root`、`train.resume` 等旧键，测试夹具不能靠恢复兼容绕过门禁。当前入口夹具迁到 `inputs.<stage>` 和 `paths.datasets.*`，冻结历史 recipe 使用独立历史配置继续跑数值基线；56 项圈定通过。
- **环境事实：**最终浏览器复验首次发现正式 5173 已停止，得到连接拒绝；按用户当次授权恢复后重新在实际 5173/8000 通过。进程存活和页面行为必须分别取证，先前截图不能代表当前服务仍在线。

## 2026-09-21：模型集成 skill 改为风险优先主流程

- **用户明确的通用要求：**每次开始或继续集成，先找出最不确定且失败后返工代价最大的关键假设，用能直接推翻它的最小真实对照验证；通过后再扩大能力、案例或运行规模。
- **适用边界：**这是集成决策的主原则，不规定模型、数据格式、固定步骤、预算或工具。缓存、Web、恢复、安装副本等只在成为当次最大风险时进入验证，不分别固化为通用强制清单。
- **处理结果：**该原则已从 `dojo-integrate-model` 的可选提醒提升到“必须检查的节点”，并删除原重复表述；具体案例仍留在改进日志。

## 2026-09-20：GeoTransolver 集成前评估

- **已证实的错误边界（静态核查一次）：**参考 YAML 写 AdamW，但训练源码按参数维数组合 Muon 与 AdamW；仅抄配置会改变优化算法。论文表头称相对 L1，CD/CL 实际为 R²，须连同正文解释读取。证据见 [评估记录](mvp/geotransolver-assessment.md) 第 3 节。
- **已证实的访问边界（本次联网核查）：**DrivAerML 非 gated，VTP 范围读取 206；SHIFT 两套数据 metadata 可见，但为 manual gated，匿名 README 401。数据目录可见不能推断完整文件可下载，更不能推断预处理可用。
- **可泛化流程候选：**版本较新的参考仓库应沿实际入口核对采样、局部几何后端、优化器和推理分块；源码来源相同不能替代实验身份一致。当前邻居展平 MLP 对顺序敏感，不能直接以另一套半径图/池化替换并声明等价。
- **待核实观察：**论文的查询/邻域预算、门控公式、局部几何描述与当前代码存在差异；体场压力在论文各表数值不同。尚未运行原模型，不能据静态差异定性算法错误或选择有利值作为已达标结果。
- **交付范围：**仅评估与证据导航，未下载完整样本、未训练、未迁移、未改环境或正式服务；没有新增数值复现证据。本条不自动升级为技能强制规则。

## 2026-09-20：GeoTransolver 小数据与论文版本补核

- **已证实的事实边界：**用户 PDF v2 没有碰撞指标，但在线 v3 新增 135 工况保险杠 / BIW 实验。此前“碰撞不属于这份论文目标”仅适用于 v2，现已增加版本限定和新推荐。详见 [评估第 6 节](mvp/geotransolver-assessment.md)。
- **已证实的身份边界：**公开 BumperBeamCrashExample 完整文件清单约 15.814 GB、VTP 约 0.540 GB，但为 131 例、固定速度；一个真实样本为 11 帧。论文 135 工况变化速度，当前配置 51 帧，不能将公开同名案例直接当作论文数据或用 0.00732 直接验收。
- **可泛化流程候选（本次核查）：**容量用文件清单加总与解压目录分别记录；usedStorage 不等于下载总字节。小型候选还要核工况、时间帧、分片和指标所属模型。Darcy 的 0.0057 属于 Transolver，不是 GeoTransolver。
- **交付范围：**本次真实解码一个 4.12 MB VTP，未训练或迁移；完整集有效性、原保险杠分片及本机求解器仍未验证。只更新专项评估，不增加通用技能强制步骤。

## 2026-09-20：PCNO 本地发布包核查

- **已证实的事实边界（本次实读）：**Code Ocean v1.0 的 8 个训练分片各含 3 例，共 24 例；另有 18 例后处理输入与既存预测，两组规范化输入的字节摘要无重叠。README 明确原数据集过大而未随包发布，不能把这些样本当作论文的 9,450 例及独立 1,050 例测试集。
- **已证实的证据边界：**`Prob_Data.pt` 的 `pres/temp` 与 `Prob_Pred.pt` 的 `Pres/Temp` 逐值相同，文件名中的 Data 不证明其场值是独立仿真真值。该配对不足以验证预测精度；未对这些重复数组计算并宣称零误差。
- **实际验证：**全部发布张量可读且数值有限；源码与锁定官方 v1.0 一致。仅在内存中改写输入/输出路径后复跑原经济评价，18 行 CSV 与作者结果逐字节相同。证据：`/Users/zonghui/work/project_simulation/dojo_train/pcno/local-package-assessment/assessment.json`，生成结果在同目录 `economic-rerun/`。
- **范围与流程候选：**本次只完成数据检查和经济评价复跑，未训练、未执行网络预测、未迁移 Dojo，也未复现论文精度。单次观察提示核对参考结果的数据身份与实际入口；不新增通用技能要求。


## 2026-09-20：GeoTransolver 原始数据归档与全量审计

- **已证实的错误边界（单次真实原函数执行）：**PhysicsNeMo 保险杠读取正则在 Run100 的 11 个位移数组中只匹配 t0，输出仅一帧位置。问题已写 `error.log`，计划先修隔离参考副本再建立模型对照，尚未修源码；证据见 [评估第 7 节](mvp/geotransolver-assessment.md) 及 `reference-reader-audit.json`。
- **已证实的事实修正（全量检查）：**公开 131 例中 129 例 11 帧、两例 12 帧；单例 11 帧不能推断全体。研究准备显式选择共同窗口且保留原额外帧。两个 Darcy MAT 的目标为 float64，MPS 适配不能悄悄改写原始精度。
- **可泛化流程候选：**远端摘要核对与领域内容检查提供不同证据；单文件传输停滞可在锁定版本和摘要下独立恢复，再全量核验。此次保险杠 4068 文件摘要、131 VTP 及 Darcy ZIP/两 MAT 全量检查通过，只代表所检查字段和原始文件完整性，不证明原求解器可重跑或网络可训练。
- **交付范围：**下载与计划完成，迁移/训练未启动；原数据位于用户指定 datasets，准备和训练计划仍使用 dojo_train。未新增通用 skill 要求，后续应按实施计划独立验收科学一致性。

## 2026-09-20：PCNO 参考基线启动与观察器对照

- **已证实事实（双分支真实首步）：**完整80×80×5×21数据、演示网络，在冻结参考环境中独立复放前向、裁剪前梯度、模型/优化器/调度状态，固定1e-5/1e-6容差通过。压力首轮21更新174.83秒、峰值约13.7GB；两个250轮原版运行仍未完成。证据见 [PCNO 实施状态](mvp/pcno-acceptance.md)。
- **已证实错误边界（验证工具返工一次）：**给PyTorch scheduler实例挂step观察函数会进入state_dict，造成额外状态字段；临时类级观察并恢复后双分支对照通过。原训练程序未受影响，未改科学算法。
- **可泛化流程候选：**耗时未知的原版运行可在不消费随机数的前提下保存首步、样本顺序、完整轮次状态，并独立检查观察未改变数值。当前恢复测试仅小模型，不自动推广为所有科学程序精确恢复结论。
- **交付边界：**数据及作者经济回放、参考入口和首步验证完成；完整原版训练、Dojo迁移、同预算比较与复制/安装验收仍未完成。不修改skill通用要求。

## 2026-09-20：用户明确能力沉淀与快速集成偏好

- **用户明确的通用偏好：**集成时尽可能抽取能力进入 core，不以首次接入就能证明通用性为前提，后续随使用形成通用能力；每次训练前估算本机耗时，集成检查优先选择整体小于三小时的方案。已写入 skill 并同步集成原则，保留参考性及用户具体范围，不设到点强杀。
- **实际触发与证据：**PCNO 当前原版压力/温度最近五轮中位耗时约190/186秒；250轮并行原版尚需约12–14小时，加上同预算Dojo约25–28小时纯计算。此前完整耗时在长训练启动后才说明，未在选定实施预算前充分呈现。来源为 `dojo_train/pcno/reference/{pres,temp}-250/epochs.jsonl`，本次读取时完成15/14轮。
- **适用边界：**三小时偏好覆盖本次集成检查的原版/Dojo、各分支和必要评价保存，不逐阶段重置。缩小实验不能宣称完整论文复现。本次是用户明确要求的通用偏好，不是从单个模型耗时自行推出强制规则；未调整已经启动的250轮任务。

## 2026-09-20：GeoTransolver core 优先实施中期

- **已证实错误边界：**实际运行器传入 DictConfig，需要沿公开加载入口解析后校验；VTKHDF会将含小数点时间的字段改名，现以稳定文件键和清单原名映射避免漏帧。Darcy坐标必须沿上游xy顺序，已重建而未改写旧准备证据。
- **已验证结果：**完整结构的小空间前向/梯度/更新差值为0；Darcy1000/200、保险杠124/7原数据与独立上游读取/统计审计逐值相同。数据和模型算子、状态等实际计算在core，contrib保留组合与绑定。
- **流程候选与范围：**中立能力可先服务一个模型，再随真实复用改进；本次使用合成数据及无关小网络检查独立性。尚未完成的成对短训、安装与Task不能据上述算子证据推断通过；后续结果以专项验收记录为准。

## 2026-09-20 PCNO：先集成后的短预算实跑

- 已证实的错误边界：原版温度第6次更新有NaN梯度，但nan_to_num让随后损失仍有限。两侧NaN不能认定迁移正确；已保留原失败，仅屏蔽未选黏度分支，并在同一修正版参考中复跑。修正版两分支21更新、首步梯度/分项、真实中断恢复及固定结果消费通过。证据 `.context/mvp/pcno-acceptance.md` 与外部 `code-validation/delivery.json`。这是当前单例数值问题，不推导所有模型都需要同一修正。
- 流程候选：用户要求先集成后验证，取消250轮前置；主要计算先进入core，贡献侧绑定发布语义。整体估算含参考/Dojo双分支、失败重试、推理与保存，本轮小于三小时；具体21更新不是通用要求。
- 已核实交接：实际wheel的复制案例、Task双分支训练、组件替换、K单位温降保存与下游读回；作者18例经济CSV逐字节一致。短训学习效果仍差、18例缺独立真值，不能扩大到论文精度或经济有效性。
- 待处理观察：当前工作区另有两项外流案例配置/共享脚本声明回归失败，明确单列，没有改动并行案例来让PCNO验收变绿。


## 2026-09-20：GeoTransolver 工程闭环

- **已验证结果：**完整网络和网格上每侧五轮，Darcy1250/保险杠620更新；完整200/7评价的权重、损失历史和预测最大差值均0。CPU固定容差未放宽，真实安装环境的直接执行/Task、两步恢复到三步/连续三步、固定结果与能力替换通过。证据 `.context/mvp/geotransolver-acceptance.md` 及外部 `installed-replay-final`。
- **已证实错误边界：**Task同一输入槽改绑共享bundle时，过期继承引用遮蔽新共享记录，派生只带清单；已修复通用路径匹配并用真实准备/权重复制、隐藏来源后执行证明。邻域缓存仅核坐标不足，补充准备时参数、形状及索引范围核验。实现中旧快照缺新增cache_spec明确失败，最终从当前wheel重复制验收；未修改历史证据。
- **可泛化流程候选：**数值一致性与独立安装/资产搬移覆盖不同交接；小网络可验证core能力，正式全网格成对短训可验证迁移，而资产复制需实际隔离来源再读回。这是本次单项工程证据，不把更新数、用例数或固定目录提升为通用要求。
- **科学边界：**Darcy相对L2约0.04817、保险杠参考归一化MSE约0.08361，支持短训学习变化；公开131例与论文135例不同，原Transolver0.0057也不是本轮成绩。论文复现保持未完成，工程结果不替代科学目标。


## 2026-09-21：FLARE++ 可选组件

- **已证实的错误边界｜比较输入布局也须一致。** 上游非连续输入与本地连续 clone 有微小差异；保持相同切片布局后，前向、梯度、更新逐值一致，未放宽容差。测试夹具修正与父目录问题见 error.log。
- **已证实的错误边界｜组件扩展不等于全局架构建议。** 用户纠正建议层级；本次只提供 core 可选注意力，调研主建议改为跨模型/上下游整体证据。
- **可泛化的流程候选｜单组件来源与安装验证。** 锁定原类、独立公式、更新恢复和实际 wheel 用户脚本对照；[组件验收](mvp/flare-attention-acceptance.md)43项通过。适用于明确授权的独立组件，不替代完整模型复现。
- **待核实观察。** 完整模型精度、内存和时间收益未测。

## 2026-09-21：PCNO 泛化前的标签—物理算子一致性诊断

- **已证实事实（真实标签与独立算子）：**双圆柱候选中心差分散度与来源交错网格前向通量差分不等价。按Lilypad公开表达式对本机5训练/1验证共6000帧、内部流体有效域检查，散度/速度梯度RMS由抽样中心差分约0.085降至全轨迹前向差分5.56e-6—6.39e-6；解析例与gradcheck通过。公开求解器版本不冒充生成数据时修改版，尚未训练或验证完整动量/结构方程。
- **待核实观察：**Darcy16例在候选调和面系数逐点残差中有明显离散不一致，数组边缘也不逐值为零；6个测试函数的弱形式对称归一化缺陷≤0.00347不能证明完整PDE等价。额外Kcoeff字段与来源离散语义未闭合，暂不批准逐点零残差/硬零边界训练。
- **流程候选与证据：**先将真实标签送入候选物理算子，核对位置、单位、mask和离散表达式，再决定是否加入损失；小残差不能独自证明物理身份。报告 `/Users/zonghui/work/project_simulation/dojo_train/pcno/physics-audit-20260921/REPORT.md`。本次仅诊断，未改模型或原始数据，未训练、未产生大型副本；不将个案差分方式固化进skill。


## 2026-09-21 GeoTransolver 外流基础接入

事实与证据：ShapeNet-Car 双域、NASA CRM 工况分支按相同完整基础网络各五轮，独立上游数值定义与 Dojo 权重、损失及全部预测最大差0；输入统计另做NumPy核验。安装后Task、恢复和全点推理逐值一致，独立post及L1误差场扩展实跑。证据见 [外流验收](mvp/geotransolver-aero-acceptance.md) 和该文引用的运行根。

已证实错误边界：物理网格已经交付时仍强制回读原始connectivity，会使仓库外物理数据消费失败；现已优先用物理VTKHDF并保留原实体ID。自动清理未使用导入可能删除公开转出别名，本次已由收集失败发现并修正。

流程候选（本次验证）：共享准备数组需独立统计/字段/身份检查，数值参考须独立加载算法；0差值证明固定算术路径一致，不等于科学精度。查询分块全覆盖与全场前向分别报告。历史资产清理后重新核验可用文件，仅补做必要样本。

未决边界：仍是小样本数据迁移研究，非DrivAerML/SHIFT论文复现；无Web登记。扩大回归遇到已有帮助指引措辞断言漂移，保持其他任务文本不变，据实记入验收；未从一次观察自动修改skill规则。

## 2026-09-21 — PCNO圆柱方法变体

- 已证实事实：官方CylinderFlow 8/2/2完整轨迹可按HTTP范围和TFRecord CRC恢复；原生P1三角梯度在10条训练/验证轨迹上的散度/梯度比为5.57%—7.14%，未通过事先约定1%门槛。因此未训练该案例，未启用未验证映射，也未读取测试标签调整算子。不可据此断言原CFD数据不满足不可压缩方程。
- 已证实事实：Double Cylinder的来源相符前向通量散度支持本轮训练。500更新监督与物理对照中，物理组散度约低23.32%，三个流场分量RMSE略升；工程通过不等于物理项提高预测精度。单种子、单验证轨迹，不外推论文水平。
- 工程证据：78项最终圈定测试通过；真实wheel外部direct/Task 10更新权重最大差0，实际构造器替换、8更新预算及速度模长保存读回通过；原网格2/4/10更新恢复与连续运行一致。
- 已证实错误：分叉检查点标签、结果资产种类必须使用已有writer合同；两次入口失败已修正并复验。固定结果独立后处理出现1.73e-18归约差，按事先约定容差通过，不改科学算法或放宽门槛。
- 流程候选（单次）：先测正式网络后再确定预算。本轮每更新约0.23秒，远低于最初粗估；正式公共运行账本累计约535.68秒。不要把原地热网络成本外推给二维变体。
- 清理：按明确生成路径回收1,164,503,510字节；保留原始来源、最终权重和必要恢复状态、全部指标及两个代表性窗口，清理后完成模型和固定结果读回。实际证据见`.context/mvp/pcno-cylinder-acceptance.md`及其中的实验根。

## 2026-09-21 — CylinderFlow散度诊断复核

- 已证实事实：6000帧独立闭式梯度、边通量积分复现原P1比值，未发现实现错误；全域净边界流量/流入量最大6.20e-8。局部P1残差比不是求解器残差或质量损失率，全域平衡也不能替代局部连续性。
- 已证实事实：解析上严格无散的曲场在原网格、原有效域线性重建后仍得到7.55%—7.90%，半边长采样后约减半。原1%候选约束门槛不能直接作数据质量门槛，曲场重建误差必须与算术正确性区分。
- 流程候选（本次）：仿射场/gradcheck证明实现正确的范围有限；面向来源数据的物理项，可增加曲场采样对照、局部与全局守恒分列。尚未证实原COMSOL局部残差的具体成因；不自动改skill、不换算子宣布通过。证据见[圆柱验收复核](mvp/pcno-cylinder-acceptance.md#2026-09-21-散度计算独立复核)。

## 2026-09-22：用户确认的集成 skill 精简修订

- 依据本日志 2026-09-21 GeoTransolver 外流记录及[外流验收](mvp/geotransolver-aero-acceptance.md)，经用户明确确认，将参考独立运行、共享输入另核验加入数值对照节点；将复制后隔离旧路径、区分缺文件与多余依赖合并到独立阶段交付提醒。零差值不是错误，仍区分迁移一致与预测精度。
- 保留三部分结构及按风险选择的边界，不固化案例、工具或测试数量。仅修改技能文字，无新增训练或科学验证；技能格式检查和现有研究导航链接测试通过（1 项）。

## 2026-09-22：JOREK recipe 工程验证与代码复用

- 已证实事实：复制WDNO完整时序案例并经用户组件改写，JOREK baseline完整500epoch、独立准备、梯度及恢复通过；未发现baseline框架表达阻碍。旧安装已有HDF5读取/矩统计/数组保存，先前缺失判断错误，数据教程漏入口已纠正。
- 流程候选：复制后先贯通配置容器、checkpoint与资产登记再启动完整训练。本次完整训练结束后才发现资产kind误用，保留失败并以原权重重试；框架边界须实际执行验收。
- 未决观察：14.33%是调用行占比而非来源复用；完整复制example也只计62.94%。新版Skill已按用户要求调整，主控工程成功尚不能证明新Agent利用率提高。完整293行映射与证据见[jorek recipe验收](mvp/jorek-rmhd-recipe-proof-acceptance.md)。

## 2026-09-22：经典网络可拆装规划与技能同步

- 用户明确确认的设计偏好：计算块、可复用网络子结构和完整中立架构都可进入core；完整架构真实复用公共组件并允许局部拆装，粒度高于机械单算子包装，不强制统一模型协议。设计正文更新至架构第5.2节；[主计划](../.cursor/plans/classic-networks-main.plan.md)及三个子计划记录本批范围与唯一维护方。
- 处理结果：A负责前馈/循环，B负责卷积/残差/多尺度，C负责注意力/图并复用A前馈；主控归并、重组核查后顺序训练。技能只吸收通用抽取与组合证据原则，具体模型、三个案例及十三组合预算保留在本批计划，不固化为通用清单。
- 文档证据：四项架构导航/PRD检查通过，技能格式校验通过；四计划骨架、技能引用与本地链接核对通过。跨文件审阅统一示例路径、编码器返回值和掩码语义，未新增网络源码或运行训练。
- 适用边界：这是用户确认的设计与计划，尚无新增数值或科学证据。经典重建及三类跨网络重组仍待实现与实跑，不能称可组合性已经验证；本次不新增算法错误结论或把候选流程当成实验事实。

## 2026-09-22：经典网络拆分与交接（实施阶段）

- 范围：按用户批准先由三组并行实现core block/网络阶段/完整模型，再主控串行十三组验证；本批缩小结构/功能验证，不声明论文精度。过程与证据见 `mvp/classic-networks-acceptance.md` 及外部classic_networks实验根。
- 已证实错误边界：mask插值接近1不能替代“参与单元全部顶点有效”；解析回贴用例复现后改为先筛有效单元。DictConfig必须在贡献配置边界解析再存JSON。检查点标签使用writer公开latest/best约定，初值通过命名空间区分；逐字段MSE的复合单位必须括号平方。三个实际入口测试及搬移后独立post已复跑。
- 已证实状态边界：只初始化Torch无法使完整Python/NumPy随机状态在重复实验中一致；复用现有seeded_randomness后完整恢复检查闭合。没有新建第二套随机状态管理。
- 待泛化流程候选：MPS三维ResNet/U-Net出现长轨迹参考偏差，最小“同模型重复计算”已检出反向浮点非确定差异，CPU重复与独立参考均为0；据此显式调整严格对照设备，而非改变容差或算法。此结论只覆盖当前硬件/框架/网络，不推广为所有MPS算子不可靠。
- 组合审查：前馈同时服务MLP、注意力和图更新；公开阶段及完整模型均有实际替换调用、参数注册、状态读回证据。三重组完整真实训练和安装结果以后续专项验收状态为准，本条不提前宣称通过。

本切片最终续接：十三组合均100更新，参考完整预测与全状态恢复通过；三个真实重组及六例独立wheel/Python/Task、准备/权重/结果搬移和独立post通过。Transformer另纠正参考ReLU零点子梯度，显式公式验证与原生PyTorch长轨迹参考分开记录；不改生产算法或固定容差。圈定274项的3个文档门禁失败已在18项受影响集合中修正闭合，零skip；具体设备及覆盖范围以专项验收为准。

## 2026-09-22：算子与传统代理分工规划

- 用户确认的范围调整：PINN不作为独立模型，复用已有物理约束；算子和传统模型均审查计算块、可复用阶段与完整架构，训练/拟合、推理与状态保存按实际计算归属。LightGBM可选官方后端不冒称内部建树块已抽取。
- 处理结果：[主计划](../.cursor/plans/operator-surrogates-main.plan.md)与A/B/C子计划明确唯一维护方、四类数据、物理损失适用性核查、十一组合及扩展累计预算；三组计划分别写入，主控核对交接。当前仅规划，无本批新增数值、训练或安装证据。
- 待验证候选：用两种算子复用同一物理损失，使用POD与可替换系数代理检验非梯度拟合、可微解码和普通对象推理边界。上述计划不代表组合已通过，不升级为全仓固定模型或测试清单；本次不修改技能通用正文。
- 文档核对：四计划骨架和技能指派、21个本地链接、5段Python示例语法检查通过；两项现有仓库入口/研究导航测试通过。交叉审阅已统一B/C职责、多项式接口和训练配置键，明确FNO不能任意空间分块、Kriging固定状态求解归training而公开条件预测归modeling。这些是规划一致性检查，不是新增模型的功能测试。

## 2026-09-22 算子与传统代理执行交付

本批11组合真实参考/Dojo对照与八个独立wheel/Task案例通过，209项圈定通过、CUDA一项跳过；证据见`mvp/operator-surrogates-acceptance.md`及实验根summary/installed/JUnit。

已证实错误边界：小FP64公式对照未覆盖真实FP32非连续查询的Linear/多输出归约路径；固定参数逐层定位后修正，原容差重训通过。GLS默认有限差分放大数值微差，独立解析梯度经中心差分验证后两组优化明确收敛。批预测必须保留复用缓冲快照，状态文本及上下文也需完整性检查；路径不同不能证明训练/测试内容独立。

科学适用性：Darcy最外圈标签非零，未把离散外圈冒充真实零边界，改用来源确认的最大值原理非负约束。FNO真实更新发生变化，DeepONet此运行约束不活跃；不能由同一loss可调用推断必然改善精度。

流程候选：已有公开执行能组合不同学习语义，非梯度拟合不需要统一神经训练协议；状态读回、树增量、优化器恢复分别证明。技能只补这一通用边界，案例参数、数量及诊断细节不变成全仓要求。以上为本批实测，不自动推断任意架构重组或论文复现完成。

## 2026-09-24：基础能力的 Agent 帮助交付

已核实缺口：新能力源码 API 已生成，但 Guide/教程未同步呈现普通拟合、预测和状态路径。补齐基础模型三种组合尺度的实际 API 导航与五类教程；以文档实跑、检索及独立 Task wheel 离线导出核验，范围见 [帮助验收](mvp/foundational-agent-help-acceptance.md)。

用户明确的通用要求：每次变更考虑 Agent 帮助影响，公开用法变化同步文档；已写入 AGENTS、集成技能和相关编写规则。生成 API 覆盖与 Agent 能发现并正确使用是不同检查；此轮验证资源与例子，不推断新 Agent 的使用率或科研精度改善。
