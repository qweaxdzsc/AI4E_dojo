<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="1" skipped="0" tests="2" time="14.681" timestamp="2026-09-22T02:04:32.773908+08:00" hostname="zonghuideMacBook-Pro.local"><testcase classname="tests.integration.test_trainprep_split" name="test_open_dataset_applies_random_split" time="0.006"><failure message="ValueError: 重划分片找不到样本 c">tmp_path = PosixPath('/private/var/folders/c4/hryn27sx61b7y785lvj4pkqc0000gn/T/pytest-of-zonghui/pytest-903/test_open_dataset_applies_rand0')

    def test_open_dataset_applies_random_split(tmp_path):
        path = write_physical_manifest(tmp_path, {"train": ["a", "b"], "test": ["c"]})
&gt;       data = open_dataset(
            {
                "train": {"manifest": str(path)},
                "trainprep": {
                    "split": {"method": "random", "seed": 0, "counts": {"train": 2, "test": 0, "eval": 1}}
                },
            }
        )

tests/integration/test_trainprep_split.py:153: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.12/site-packages/ai4e_core/applications/aero_cfd/trainprep/preparation.py:83: in open_dataset
    apply_declared_split(index, config, overlay)
.venv/lib/python3.12/site-packages/ai4e_core/abilities/data/source/split.py:244: in apply_declared_split
    index.remap_partitions(resolve_split(index.partitions, split))
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = &lt;ai4e_core.abilities.data.source.manifest.ManifestIndex object at 0x10e7ff2c0&gt;
partitions = {'train': ['a', 'c'], 'eval': ['b']}

    def remap_partitions(self, partitions: Mapping[str, Sequence[str]]) -&gt; None:
        """按样本名重挂分片；张量路径不变，空分片不进入索引。"""
        by_name: dict[str, list[dict]] = {}
        for (_partition, sample), record in self.records.items():
            by_name.setdefault(sample, []).append(record)
        rebuilt: dict[tuple[str, str], dict] = {}
        remapped: dict[str, list[str]] = {}
        for name, samples in partitions.items():
            kept: list[str] = []
            for sample in samples:
                identity = str(sample)
                candidates = by_name.get(identity, [])
                # 同名样本可以来自不同原始来源；目标 test 优先消费原 test
                # 记录，训练/验证优先消费原 train/validation 记录。
                record = next(
                    (item for item in candidates if item.get("partition") == name),
                    None,
                )
                if record is None and name == "test":
                    record = next(
                        (item for item in candidates if item.get("partition") == "test"),
                        None,
                    )
                if record is None:
                    record = next(
                        (item for item in candidates if item.get("partition") != "test"),
                        None,
                    )
                if record is None:
&gt;                   raise ValueError(f"重划分片找不到样本 {identity}")
E                   ValueError: 重划分片找不到样本 c

.venv/lib/python3.12/site-packages/ai4e_core/abilities/data/source/manifest.py:68: ValueError</failure></testcase><testcase classname="tests.integration.test_task_installation" name="test_wheel_install_outside_checkout" time="14.539" /></testsuite></testsuites>