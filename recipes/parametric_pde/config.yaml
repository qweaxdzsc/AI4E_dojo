case: neumann_diffusion
dataset:
  name: neumann_diffusion
run_root: ../runs
trainprep:
  execute: true
train:
  execute: true
  device: auto
post: {}
data_root: ../data
pipeline:
  stages:
  - rawprep
  - trainprep
  - train
  - infer
  - post
inputs:
  rawprep:
    manifest: ../datasets/manifest.json
  trainprep:
    dataset: ../datasets/manifest.json
  train:
    dataset: ../datasets/manifest.json
    preparation: null
    resume: null
  infer:
    dataset: ../datasets/manifest.json
    preparation: null
    checkpoint: null
  post:
    results: null
